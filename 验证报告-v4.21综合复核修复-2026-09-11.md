# v4.21 修复验证报告（综合复核 P0 批次 · 本地验证）

- **日期**：2026-09-11（晚）
- **依据**：《综合复核报告-2026-09-11.md》《综合复核报告-实际使用场景-2026-09-11.md》（已归档至 `docs/archive/`）
- **范围**：云端 `cloud_relay_v2.py` + 面板 `dashboard.html` + 扩展 `background.js` + Android `DialService.kt`
- **状态**：代码已改完并全部本地验证通过；**线上（101.34.65.254:35430）尚未部署**
- **改动文件**：`cloud-relay/python/cloud_relay_v2.py`、`cloud-relay/python/dashboard.html`、`AutoDial-Extension/background.js`、`android-app/.../DialService.kt`、`CHANGELOG.md`

---

## 一、修了什么（逐项对照复核报告核实后执行）

| # | 问题（复核报告编号） | 核实结论 | 处理 |
|---|---|---|---|
| 1 | C-01/X-01 REST 限流 60/min/IP 被扩展轮询打爆，已实际拦截上门登记（线上 16:54 日志） | ✅ 属实（:567-579, :1471；background.js 5s 轮询） | **已修**：限流按端点分级——认证类 60、轮询类 240、业务类 600（min/IP）；扩展轮询 5s→30s + 429 退避；面板未登录停刷 |
| 2 | P0-B 通话记录/事件上报 0 产物 | ✅ 属实（logEvent 全仓库零调用；calls 上报三卡点） | **已修**：logEvent 接线（dial/sms 结果上报）；权限缺失/上报失败打日志；首传改 DESC 优先最近记录；水位线 DESC 取本批最小 id |
| 3 | P0-C 生产库 100 台 ZZLOADTEST 压测设备 | ✅ 属实（本机库也有 50 台 StressTest-*） | **待拍板**：涉及删生产数据，需管理员确认后手工清理 |
| 4 | C-02/D-2 批量导入超约 20 行必失败 | ✅ 属实（websockets `MAX_LINE_LENGTH=8192` 实装验证） | **已修**：云端放行 POST + body 读取；`/api/v1/visits/batch` 支持 POST body；面板改 POST + 20 条/批 |
| 5 | "手机没连上却显示已拨出"（G-3） | ✅ 属实（PC 返回 200{success:false}，扩展只看 HTTP 码） | **已修**：dial/hangup/sms 三处读响应体，success:false 如实报错，dial 失败落云端兜底 |
| 6 | A-4 Android 14 前台服务权限缺失 | ❌ **复核为已修** | 无需改动（specialUse 类型 + manifest 声明齐全，StartForeground 不会再炸） |
| 7 | B 配对码 deviceId 遗留问题 | ❌ **复核为已实施**（v4.16） | 无需改动（LAN/Cloud hello 均带 deviceId，云端以 deviceId 绑定） |
| 8 | 主线1 局域网直连死链路 | ✅ 属实（PC 只监听 127.0.0.1） | **待拍板**：建议砍入口（手机端删"仅局域网"策略），未动代码 |

### 计划外发现（本批新增修复）

- **init_db 全新库初始化必炸**（v4.17 引入的潜伏雷）：`idx_call_records_dial` 索引建在 `create_call_records` 之前，全新 DB 首启必抛 `no such table` → 落入 `:memory:` 降级分支 → 数据全落内存、重启即丢。线上因旧库带表未触发。**已修**（两处分支均调整为先建表后建索引）。

---

## 二、验证方式与结果

| 项 | 方式 | 结果 |
|---|---|---|
| 云端 Python 语法 | `python -m py_compile` | ✅ |
| 回归测试 | `pytest`（103 用例） | ✅ 103 通过；2 失败为历史遗留 async 用例需 pytest-asyncio，**改动前同样失败**，与本批无关 |
| 限流分级 | 单元级验证（导入模块直调 check_rest_rate_limit） | ✅ 三档配额精确生效（600/240/60）、桶互相独立、被拒请求不占槽、localhost/空 IP 豁免 |
| 批量导入 | **真启动服务器**（隔离临时 DB）+ urllib POST 30 行 | ✅ `inserted=30 skipped=0`，落库核对 30 行；GET 小批量兼容（inserted=1） |
| init_db 修复 | 隔离全新 DB 启动 | ✅ 无异常，表/索引齐全（修复前必报 no such table 并降级内存库） |
| 扩展 JS | `node --check` background.js | ✅ |
| 面板 JS | 两段内联 `<script>` 提取后 `node --check` | ✅ |
| Android Kotlin | `gradle compileDebugKotlin`（离线+本机 SDK） | ✅ BUILD SUCCESSFUL（验证用临时改动已还原：kotlin_version/local.properties） |

> 验证细节：POST body 经 `_PeerProtocol.read_http_request()` 放行（websockets 16.1.1 实装 `read_request()` 硬编码只接受 GET）；`content-length` 由协议子类在 `process_request` 前读入 `_request_body` ContextVar，不碰 WS 握手路径（method=GET 行为不变）。

---

## 三、部署提示（下次上线时）

1. 云端两文件：`cloud_relay_v2.py`、`dashboard.html` → 按既有流程（备份 → MD5 对比 → `py_compile` 预检 → restart → 健康检查）
2. 扩展目录整包更新后让员工浏览器自动升级；无需后端配合
3. Android 需重新打包发 APK（logEvent 接线生效后，`phone_events` 才开始有数据；`call_records_raw` 需手机 WS 在线且已授 READ_CALL_LOG）
4. 部署后观察项：`REST_RATE_LIMITED` 日志计数应大幅下降；`phone_events` 开始有行；面板批量导入 >20 行成功

---

## 四、管理员拍板结果与本批执行情况（2026-09-11 晚）

| 事项 | 管理员决定 | 执行结果 |
|---|---|---|
| 压测假数据 | 直接删，不备份 | 本地 50 台 `StressTest-Phone-*` 已删（phones 50→0，含关联表）；**线上 100 台 `ZZLOADTEST-*` 本机无 SSH 凭据未执行**，命令见下方 |
| 局域网直连 | **保留，修通** | ✅ 已修（见下） |
| 登记"无声丢失"（C-2） | 不管（CRM 会补回） | 未动 |
| PC 一错就退出（P-1） | 要修 | ✅ 已修 |
| 同步空数组清空本地（A-3） | 属正常设计 | 未动 |
| PIN 串号（E-1） | 解释即可 | 已解释（见下），是否修复待定 |
| 双击双拨（E-3） | 手机只拨一次即可 | ✅ PC 直连路径已补 5 秒同号去重（云端原本就有） |
| 本地打包 APK | 不要 | 未打包，仅改代码 |

### 局域网直连修复明细（三处死因全修）

1. `main.js:830` 监听 `127.0.0.1` → `0.0.0.0`（手机 UDP 发现 PC 局域网 IP 后连不进来的第一层墙）
2. `server.js` WS `verifyClient` 回环 Host 强制 + 空 Origin 一律拒 → 改为"带 Origin 必须可信（防网页静默拨号，防线不变）；无 Origin 放行（手机 OkHttp 不发 Origin），PIN 握手兜底"（第二层墙）
3. HTTP 层回环校验保留不动（扩展只走本机，LAN 手机不走 HTTP）
4. Android"检查防火墙"误导文案 → 改为"同一 WiFi + AutoDial PC 运行中 + 首次启动允许防火墙"
5. 注意：电脑端首次启动监听外网卡时 Windows 会弹防火墙授权框，**点允许**；之前被拒过的电脑需在"允许的应用"里放行 AutoDial/Node.js
6. Go 版 PC 端同样的监听问题（server.go:260）**未动**——等 Go 版去留拍板

**验证**：Node `--check` 通过；Android `compileDebugKotlin` 通过（验证用临时改动已还原）。

### 线上假数据清理命令（管理员在服务器上执行）

```bash
sudo cp /opt/autodial/visits.db /opt/autodial/visits.db.bak-$(date +%Y%m%d)  # 顺手备份一条（不想要可省）
sqlite3 /opt/autodial/visits.db "DELETE FROM phones WHERE device_id LIKE 'ZZLOADTEST-%';
DELETE FROM call_records_raw WHERE device_id LIKE 'ZZLOADTEST-%';
DELETE FROM phone_events WHERE device_id LIKE 'ZZLOADTEST-%';
DELETE FROM phone_daily_stats WHERE device_id LIKE 'ZZLOADTEST-%';"
sudo supervisorctl restart autodial
```
（若服务器没装 sqlite3 命令行，先 `sudo apt install -y sqlite3`；重启是为了清掉进程内的 phones 内存缓存）

### PIN 串号机制解释（人话）

拨号指令里"用哪部手机拨"由**电脑插件**决定，手机 App 里输的 PIN 只决定"这台手机连到哪个组当执行器"。插件认的不是手机，而是**这台电脑浏览器里 CRM 登录的是谁的账号**。所以：换人用电脑时只要 CRM 登录的还是前一任（浏览器没退出登录），插件检测到的坐席号就是前一任，拨号请求带的就是前一任的 PIN——新员工用自己手机却把客户记/拨到前一任名下；前一任手机不在线时新员工甚至拨不出去。只要"每人固定电脑 + CRM 登录自己的号"，这个风险不会触发；共用电脑才需要修（修法：插件检测到坐席号变化时同步切换 PIN）。是否修等拍板。

---

## 五、第二梯队修复（v4.21.2，2026-09-11 深夜）

> 管理员逐项拍板：8 修、10 配对成功再踢、11 修、12 修、13 修；9 正常不动、14 Go 版不动、15 安全收尾不动。

| 项 | 决定 | 处理 |
|---|---|---|
| 8 切服务器不重连 | 修 | ✅ "当前服务器"排到队首 + 已连接时立即重连（`switchCloudServer`） |
| 9 后台保活 | 正常 | 未动 |
| 10 授权误踢 | 配对成功再踢 | ✅ 踢旧机推迟到授权成功之后；实测 B 被拒后 A 仍在线可收拨号 |
| 11 导出卡全员 | 修 | ✅ 6 个重端点 DB 段移入线程池（含两个 ≤20 万行导出） |
| 12 凭据出网址 + 刷新打回第1页 | 修 | ✅ 登录/加账号/改密 POST body、导出 Bearer 头 + blob；软刷新不重置分页/筛选/展开 |
| 13 面板小毛病一批 | 修 | ✅ hash 路由/自定义确认弹窗/改密下拉/删假详情列/列宽 72/表头 12px/对比度 AA/请求序号/横幅错误计数/未登录中性文案 |
| 14 Go 版去留 | 不动 | 未动（同 LAN 监听问题一并保留） |
| 15 安全收尾 | 不动 | 未动 |

### 本批验证记录

- pytest：103 通过（2 失败为历史遗留 async 用例需 pytest-asyncio，改动前同样失败）
- E2E（真启动服务器，隔离 DB）：
  - POST-body 登录 / 加账号成功；Bearer 头导出 200（CSV BOM）且无 token 时 401；GET 登录兼容仍通过
  - C-4：A 正常入组 → B 用他人 PIN 被拒 → A 仍在组内且能收到拨号指令
- 面板两段内联脚本 `node --check` 通过；云端 `py_compile` 通过；Android `compileDebugKotlin` 通过（验证用临时改动已还原）

### 仍未动 / 后续

- 复核实况为"正常/已修"的项：登记无声丢失（C-2，CRM 会补回）、同步空数组清行为（A-3 设计）、Android 14 前台服务、deviceId——均按拍板不动
- 第三梯队已拍板不动的：Go 版去留、安全收尾（默认口令/9001/8090/allowBackup/p12）
- 面板剩余低优项（本轮未做，可后续）：手机历史缓存永不过期、isBusyEditing 手动恢复边界、原生 `alert` 提示残留、时区（需先确认服务器时区）

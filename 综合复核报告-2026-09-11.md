# 综合复核报告 —— 各端实际使用问题（v4.19/v4.20 更新后）

> 复核日期：2026-09-11（晚，针对合入 v4.19/v4.20 后的当前代码）
> 复核方式：逐条回源码验证（云端/面板/安卓/两套 PC 端/扩展），关键结论全部标注当前文件行号
> 使用背景：公司内部约 20 人使用，云端为个人私有服务器（101.34.65.254:35430），单管理员，**便捷优先于安全，但零体验代价的安全问题一并报告**
> 与《代码审查报告-实际使用问题-2026-09-11.md》的关系：沿用其 A/C/D/E/G/P 编号体系；本报告为该报告在 v4.19/v4.20 合流后的**状态复核 + 新发现**，行号均为当前代码最新位置
> 严重度：P0 = 正在发生/核心链路失效/数据错误丢失；P1 = 高频或明显影响体验；P2 = 边界与打磨

---

## 〇、结论速览

**好消息（本批已修好并实测部署）**：管理面板第一批整改 8 项 + P0 修复 3 项全部上线验证通过（统一请求层/401 横幅、假保存移除、通话导出服务端全量、密码框遮罩、自动刷新暂停与跳过编辑、表格横向滚动、搜索防抖），云端广播 TypeError 修复，面板数据通道 11 接口全部健康。

**坏消息（线上正在发生，必须先处理）**：
1. **REST 限流按 IP 计数正在拦截真实业务**——16:54 真实员工流量中 `/api/v1/visit`（上门登记）被 429 拦下；扩展端"授权轮询"一项就是最大单一来源（约 12 次/分/台，20 台 ≈ 240 次/分，超限 4 倍）。
2. **通话记录/事件上报链路 0 产物**——生产库 `call_records_raw=0`、`phone_events=0`。`logEvent()` 是死代码（全仓库零调用），calls 上报受 WS 在线门控 + 权限静默 return + 水位线残留三重卡点。
3. **生产库被压测数据污染**——手机管理页 101 台设备里 100 台是 `ZZLOADTEST-*` 假设备，待你确认后清理。

---

## 一、已修复并验证（v4.19/v4.20，部署实测通过）

| 项 | 内容 | 验证依据 |
|---|---|---|
| D-1 | 面板假"保存设置"移除，端口/日志改只读显示真实值 | dashboard.html:798-806, 2191-2195 |
| D-2 | 统一请求层 + 401/429/断网全局横幅 | apiGet :1271-1296 |
| D-3 | 通话导出改服务端全量（原只导当前页 50 行） | :1649-1664；新增 `/api/v1/calls/export`（云端 :2849-2937，鉴权/BOM/防公式注入完整） |
| D-5 | 自动刷新：编辑中跳过 + 可暂停/恢复 | :2215-2265（浏览器实测三路径全过） |
| D-6 | 管理员密码框 type=password | :731, 744 |
| D-7 | 4 处内联编辑静默 catch 补报错+重拉 | :1563-1589, 2144-2160 |
| C7 部分 | crm_id 唯一索引已落库（竞态兜底，见 C-7 备注） | cloud_relay_v2.py:227 |
| — | 新设备加入已有组广播 `list(pcs)\|list(phones)` TypeError 修复 | :868 |

---

## 二、线上正在发生的 3 件事（P0，本周必须处理）

### P0-A：REST 限流 60/分钟/IP 正在拦截登记与拨号
- 云端 `MAX_REST_PER_MINUTE=60`，口径纯 IP（cloud_relay_v2.py:567, 570-576, 1441-1442），**所有 /api/v1/ 端点共享同一 IP 配额，无业务端点放宽**。
- **扩展端是最大单一来源**（本轮量化）：`background.js:86` `setInterval(pollAuthRequests, 5000)`——只要 CRM 页开着就稳定 ~12 次/分/台打 `/api/v1/auth/pending`；MV3 休眠后由 15s alarm 唤醒重新武装，实际不受休眠影响。5 台电脑即打满 60/min，20 台 ≈ 240/min。
- 放大器：面板锁定态/401 后**仍每 15 秒全量轮询**（dashboard.html:2256-2265 无登录态判定，每轮 4 个必 401 请求 ≈ 16 次/分）；扩展 `flushCloudVisits` 在 429 环境下失败留队→下次登记全量重试（滚雪球，background.js:352, 401）。
- **线上证据**：16:54 `REST_RATE_LIMITED path=/api/v1/visit`（真实员工登记被拦）+ `auth/pending` 连续 3 次 429。
- **修法（不动用户体验）**：① 限流改"认证敏感端点严格 + 业务端点按 PIN/大幅放宽（如 600/min/IP）"；② 扩展 auth 轮询降频到 30-60s（放 `chrome.alarms`）+ 429 退避；③ 面板 401/锁定态停轮询。

### P0-B：通话记录/事件上报从未产出（面板"通话记录"页永远是空的）
- **`phone_events=0` 的确定性根因：`logEvent()` 是死代码**——定义在 DialService.kt:921-939，全仓库零调用（grep 证实）。
- `calls=0` 的三个候选（按可能性排序）：
  1. **isConnected 门控**：DialService.kt:833 `if (!isConnected) { scheduleNextSync(); return }`——上报要求 WS 已认证在线；而登记走 HTTP 不需要，与"登记正常但 calls=0"完全自洽。严格模式下未注册/待授权手机永远到不了 CONNECTED。
  2. **READ_CALL_LOG 权限未授权**：DialService.kt:846-847 权限缺失直接 return，无日志无提示；首次启动一次性申请被拒后永不重试。
  3. **水位线残留**：`last_synced_call_id`（DialService.kt:830, 860-862）不区分服务器——对老库同步过的手机切到新库后查询恒为空。
- **判别法**：查生产库 `phone_daily_stats` 是否 0 行（stats/report 同门控但不查权限）→ 0 行即候选 1 成立。
- **修法**：接线 logEvent 调用点；上报失败给日志与提示（不再裸 `catch{}`，:896）；权限缺失时在设置页给"去授权"入口；水位线与服务器绑定或允许手动重置。
- 附带发现：首传从**最老的**通话记录开始爬（:860-862 `_ID ASC LIMIT 20`，存量大的手机要数小时~数天才能报到"今天"，且先入库陈年数据污染时间线）——应改 DESC 或按日期优先。

### P0-C：生产库 100 条压测假设备（待你确认清理）
- `phones` 表 101 条中 100 条为 `ZZLOADTEST-P003…P100`（写入时间 2026-09-11 14:10，与 `test_stress_live.py` 打生产库时间吻合）。
- 后果：手机管理页展示假数据，无法判断真实设备。
- **需要你的决定**：确认后先备份 `visits.db` 再清理；同时给压测脚本加"独立 DB/`--dry-run`"约束。

---

## 三、分端复核结果（当前行号）

### 3.1 云端 cloud_relay_v2.py

| 编号 | 状态 | 位置（当前行号） | 说明 |
|---|---|---|---|
| C-1 | ❌仍存在 | :1412 起（REST 全部端点），对照 :818 | REST 处理仍同步跑 SQLite 在事件循环上；**新放大器**：两个导出端点同步 fetchall ≤20 万行+内存拼 CSV（:2551-2587, :2886-2931），导出期间所有 WS 拨号/心跳停摆 |
| C-2 | ❌仍存在 | :1404, :650-651, :1409 | 手机在线但发送瞬间掉线→没送达也不写 pending_visits，**登记无痕丢失** |
| C-3 | ❌仍存在（放大） | :2407-2410, :1641-1644, :1696-1700 | 带 pin 即可无鉴权读全部来访数据；且免鉴权分支**无 LIMIT 全量返回**（:2471），一次 GET 可拖走全量历史（含客户手机号） |
| C-4 | ❌仍存在 | :739-745 vs :753/:769-832 | 踢旧机在授权判定之前无条件执行→待授权设备顶掉该 PIN 正在线的手机 |
| C-5 | ❌仍存在 | :231-233 | init_db 失败降级 ':memory:'，每连接空库，登记/记录持续丢 |
| C-6 | ❌仍存在 | :2123-2127, :2238-2242 | calls/batch、visits/batch 仍 GET+query 大 JSON（反代下 414/截断，且明文进访问日志） |
| C-7 | 🔶部分缓解 | :2344-2364, :227 | crm_id 有唯一索引兜底；竞态撞索引会返回 500 而非 skipped；mobile+visit_time/2 小时窗口路径仍纯竞态 |
| C-8 | ❌仍存在 | :3198 | shutdown 协程内 sys.exit(0)，无法优雅退出 |
| C-9 | ❌仍存在 | :62-63, :446-448 | 日志/STATS_FILE 仍走 APPDATA or ~，Docker 下可能重建即丢 |

**云端本轮新发现**：
1. `new_device_join` 手机侧广播仍是死代码：`is_first_device` 在踢旧机之后计算（:746, :866-883），纯手机组恒为空；auth_ok 的 `newDevice` 字段恒 False。
2. 限流空 IP 放行：`_peer_ip` 捕获失败时限流形同虚设（:572, :594-597）。
3. `/api/v1/calls` 负 limit 全表泄漏：`limit=-1` 过 min() 后仍为 -1，SQLite LIMIT -1=无上限（:2805-2806, :2835）。
4. `/api/v1/visits` 非法 page（如 'abc'）静默回落数组模式，响应结构突变（:2413-2414, :2469-2473）。

### 3.2 管理面板 dashboard.html

**旧问题复核**：D-4（时区）❌、D-8（token/密码走 URL）❌、D-9（recent-clients 转义）🔶部分、D-10（手机页无分页，当前 101 台全渲染）❌。遗留清单 8 项（未登录弹"登录已过期"、空"操作"列、"详情"只弹 toast、改密码手输 ID、原生 confirm、无 URL 路由、表格最小宽 1012px、#last-refresh 残留）**全部仍在**。

**本轮新发现（8 条）**：
1. 横幅"成功即灭"会吹灭并发失败：任何一次成功都无条件清横幅（:1290），并发时"后完成的请求"说了算，错误被静默吞掉。
2. **锁定态/401 后轮询不停**：`body.locked` 或 token 过期后仍每 15s 全量刷新（:2256-2265），持续消耗限流配额（P0-A 放大器）；401 只清 token 不停表、不弹登录框。
3. 刷新重置用户上下文：通话页翻页被打回第 1 页（:1593-1595）、筛选下拉被重置为"全部"（:1816-1831, :2121-2125）、展开的历史行每 15s 被折叠且 `phoneHistoryCache` 永不过期（:1509, :1526）。
4. 凭据泄露面：登录/加账号/改密码把明文密码放 GET query（:1176, :1215, :1238）；两处导出 `window.open(带token的URL)`，token 进地址栏与浏览器历史（:1662, :1888）。
5. 批量导入把 200 条 JSON 塞 GET URL（:2067-2068），超 8KB 即 414/截断，后半批静默丢失。
6. 防抖定时器跨页存活 + 所有 load 无请求序号（慢响应后到会覆盖新数据）。
7. isBusyEditing 边界：手动"恢复"绕过编辑判定（:2246）；"已跳过（正在填写）"提示在非首页永久残留（:2260）；锁定态下判定恒 false（与 N2 叠加成无限 401 轮询）。
8. wrapDataTables 复核**无新问题**（包裹一次、tbody 重绘不影响）；唯一遗留是 92px/列下限偏大（1280 屏也横向滚）。

### 3.3 Android 手机端

**A-1~A-9 全部仍存在**（复核行号）：

| 编号 | 位置 | 说明 |
|---|---|---|
| A-1 | AndroidManifest.xml:77-90 | 无障碍服务整段注释；部分小米机型选卡弹窗无人自动点击 |
| A-2 | CloudServerSheet.kt:94-97 | "设为当前服务器"只写偏好不重连——界面切了、实际还连旧的 |
| A-3 | StatsFragment.kt:541-559 | 云端返回空数组也整体覆写本地 visit_records（清空） |
| A-4 | ConnectionManager.kt:110, 1057-1109 | 重连寄生于进程内 Handler，进程被杀即失效 |
| A-5 | MainActivity.kt:86-87 | onCreate 无条件 manual_disconnect=false，"手动断开"形同虚设 |
| A-6 | PrefCtrl.kt:60 vs ConnectionManager.kt:367 | 自动重连默认值两处相反 |
| A-7 | CallDetailSheet.kt:92-103、CallLogFragment.kt:340-353 | 通话记录页"重拨"绕过 DialEngine：不走选卡逻辑、不写拨号记录、不回执 PC |
| A-8 | StatsFragment.kt:571-577 | 同步失败只说"检查云端连接"，不带原因 |
| A-9 | DialService.kt:112-119, 215-230 | 防重复仅 messageId，无同号时间窗兜底 |

**本轮新发现（6 条）**：
1. onCreate 异常恢复路径漏重启 `startDataSync()`（:385-411 catch 分支）——一旦初始化中途异常，该设备 calls/stats 永不启动且零提示（可直接制造"通话 0"设备）。
2. 云服务器行"连接"把 `ws://`/`ip:port` 格式的云地址当局域网 IP 持久化：ConnectFragment.kt:1250-1252 → DialService.kt:443-444 存入 "ip" → ConnectionManager.kt:375 读回 → :760-761 拼 `ws://$ip:35432` 形成无效 URL，LAN 通道静默失败。
3. `simHandleCache` 永不失效（DialEngine.kt:34-42）——换卡/热插拔后 placeCall 抛 SecurityException 落入系统默认卡兜底，双卡设备拨错卡且无感知。
4. calls/batch 与 events/log 服务端完全无鉴权、不校验 device_id 归属，可伪造通话记录/事件；PIN+通话数据以 GET URL 明文传输（DialService.kt:887-889, 930-933）。
5. 补推（flushPendingSyncs）只在 WS 重连时触发（ConnectionManager.kt:915）——若设备始终到不了 CONNECTED，积压登记永远滞留本地（与 P0-B 同根）。
6. （升级项）旧 APK 与新 APK 设备键并存期间的绑定问题，随发放节奏消化（v4.16 已处理，此处仅提醒待办 B3：后台给设备预设 default_pin）。

### 3.4 PC 端 —— Electron（主力）

P-1~P-6、主线 1、短信、退出、自启、连接管理、设置写入、日志明文**全部仍存在**：

| 项 | 位置 | 说明 |
|---|---|---|
| P-1 | main.js:931-937 | 任何未捕获异常直接 app.quit()——坐席工具莫名消失 |
| P-2/P-3 | main.js:472-482 | get-info 不返回 firewall/connected 字段，首屏状态与防火墙提示是死代码 |
| P-4 | server.js:144, 170 | 每次拨号无条件写剪贴板，覆盖用户复制内容 |
| P-5 | 全文无 | 无单实例锁，双开只弹错误框 |
| P-6 | cloud.js:83-87 | 云重连 30 次耗尽后彻底停止，夜间抖动白天不自愈 |
| 主线 1 | main.js:830 + server.js:78, 278 | 只监听 127.0.0.1 + 回环 Host 校验 → **手机 LAN 直连整体失效**，手机端还在提示"查防火墙"（误导） |
| 短信 | main.js:498-510 + sms.html:569-602 | 失败/超时不回发 sms-result，窗口永久卡"等待手机确认" |
| 退出 | main.js:863-868 | "关闭即退出"分支主窗关了但悬浮条存活→无托盘残留进程 |
| 自启 | main.js:95 + package.json:16-30 | 便携版 exe 是临时解压路径，开机自启静默失效 |
| 连接管理 | phone-connection-manager.js:217, 268-272, 659/671 | 旧连接 close 杀掉新连接（removeDevice 无归属校验）；幽灵设备 lastHeartbeat=0 永不清理；LAN hello 覆盖 isCloud → 切网不能自愈 |
| 设置 | settings.js:52-53, 44-47, 76 | 非原子写+静默失败；损坏即回退默认并写回，配置全丢 |
| 日志 | main.js:75-77, 344 + pcm.js:335/418 | 明文 PIN 与短信内容进界面日志与文件日志 |

### 3.5 PC 端 —— Go（备用）

| 项 | 状态 | 位置 |
|---|---|---|
| G-1 | ❌ | main.go:13 embed `frontend/dist` 不存在 → **go build 阻断**（若仍要发布 Go 版） |
| G-2~G-4 | ❌ | udp.go:50/65/91 裸读 pinCode；app.go:426、server.go:71/103 裸读 activePin/cloudConnected；cloud.go:44-45 无锁 RMW |
| G-5 | 🔶已缓解 | 端口占用会推 server-log 并自动展开日志面板（index.html:1107, 1370-1380），仍无独立弹窗 |
| 云端唤醒队列 | ❌ | **cloud.go:169-203 的 phone_hello 不排空 dialQueue**（排空只在 server.go:421-441 LAN 路径）——"唤醒手机后排队的号码永远不拨出"，自动拨号核心功能失效 |
| 定时重连踢人 | ❌ | cloud.go:289-308 循环读 cloudConnected 必 false→踢掉刚连上的服务器，反复闪断 |
| 粘滞标志 | ❌ | cloud.go:311-313, 266-268：保存设置/重启云端后 cloudUserDisconnected 置 true 不复位，云断线不再自动重连 |
| 假在线 | ❌ | devices.go:328-340, 368-400, 423-425：Stale 设备不过滤，"手机已连接"假绿最长 120s |
| 同 PIN 抢连接 | ❌ | devices.go:264-270 + server.go:303-331：旧连接退出 defer removeDevice 会 Close 新连接 |
| SetAutoStart 死开关 | ❌ | app.go:205 前端零调用 |
| 设置路径 | ❌ | settings.go:73-77 → Roaming 根目录裸 settings.json；无 Electron→Go 迁移 |
| 日志明文 PIN | ❌ | app.go:39, 406 |
| 主线 1 | ❌ | server.go:260 + security.go:57-63 + devices.go:33-37：LAN 同样整体不可用 |

### 3.5 Chrome 扩展

**E-1~E-10 全部仍存在**（复核行号见正文）。关键三条：
- **E-1 坐席串号**（content-script.js:1571, 1625 + background.js:177, 526-527）：兜底检测把新坐席号只写 `self_phone`，`pin` 仍是旧值，换人后拨号/登记全挂上一任名下——**业务归属错误，优先修**。
- **E-3 双拨/穿透**（content-script.js:435-446, 752-758 + background.js:220-225）：浮窗无防抖连点即两拨；PC 返回非 2xx 静默穿透云端再拨一次。
- **E-5 三处裸 fetch**（background.js:67, 729, 809）：云端假死时授权/同步永久挂起。

**本轮新发现（5 条）**：
1. **拨号链接闭包固化旧号码**（content-script.js:1724-1732）：`__adHooked` 只钩一次，click 闭包捕获首次检测的号码——SPA 复用同一 `<a>` 节点切换客户后，点击"拨打"链接仍拨**上一位客户**（误拨/错拨风险，建议与 E-1 同批修）。
2. **批量同步丢弃 crm_id**（background.js:722-728）：v4.17 做的 crm_id 云端去重对批量同步不生效，跨日/补录可重复入库。
3. **补推滚雪球**：429 环境下失败留队→下次登记全量重试，队列只进不出（background.js:352, 380, 401）。
4. 多 iframe "切页清零"互相误伤：一个 iframe 切走会清掉另一个 iframe 正在展示的客户号码（content-script.js:1792-1794 + background.js:546-551）。
5. 两处无超时 fetch 补漏：content-script.js:1273（"测试连接"永久"测试中"）、background.js:202（uploadAdvisorName）。

---

## 四、安全专项（按"零体验代价优先"排序）

### 4.1 线上暴露（真实数据实测发现，建议尽快）
| 优先级 | 项 | 说明 |
|---|---|---|
| 🔴 立即 | 管理口令 `123456` | 全球最常见弱口令，双证据确认仍在用 |
| 🔴 立即 | **Supervisor 9001 对外且与 SSH 同口令** | 9001 凭据泄露=直接拿到服务器权限；1Panel 8090 同样公网可达 |
| 高 | 登录 `GET /api/v1/login?user=&pass=` | 密码进浏览器历史/中间设备日志；改 POST+body 零体验代价 |
| 🔶 近期 | 35430 纯 HTTP | PIN/token/客户数据明文过公网；中期上 TLS 或前置反代 |
| 🔶 低成本 | token 走 `?token=` | 进浏览器历史与访问日志；改 Authorization 头零体验代价 |
| 🔶 低成本 | sha256 固定盐（盐在源码） | 改强口令后风险已大幅下降，可后置 |

### 4.2 各端零体验代价安全项（顺手修，不加任何用户操作）
- **数据可伪造**：calls/batch、events/log、stats/report 完全无鉴权且不校验 device_id 归属（云端 :2123-2159）——内部 20 人风险可接受，但"同事填错 device_id 就把别人的通话记录写进别人名下"属**体验问题**，建议至少校验 device_id 与 pin 的对应关系。
- Android：`allowBackup="true"`（PIN/客户资料随系统备份带走）；日志明文 PIN/短信且可一键导出分享（FileLogger.kt:78-110 + DialService.kt:214/250/513/522）。
- 两套 PC 端日志明文 PIN/短信内容（Electron main.js:344、Go app.go:39/406）。
- 扩展：`<all_urls>` 权限过大（推广时被质疑）、content-script message 不校验 e.origin（content-script.js:1646-1650）、escHtml 不转义引号（:1473-1475，登记弹窗属性注入）。
- 发布链：`android-app/autodial-release.p12` 签名密钥已被 git 跟踪（.gitignore 有 `*.p12` 但文件被强制提交）——建议在下次大版本统一换签名时处理，别单独发版（有卸载重装代价）。

---

## 五、建议修复顺序（三批）

**第一批 · 止血（本周，直接决定"能不能用"）**
1. P0-A 限流治理：业务端点按 PIN 放宽 + 扩展 auth 轮询降频+429 退避 + 面板锁定态停轮询（三处各 10-30 行）
2. P0-B 通话/事件上报：先查 `phone_daily_stats` 定位候选 1；接线 logEvent；补推链路兜底
3. P0-C 生产库假设备清理（等你确认）
4. C-2 登记推送改"先落 pending、送达再删"（堵无痕丢失）
5. Electron P-1：未捕获异常不再退出
6. A-3 同步空数组不清本地
7. E-1 坐席串号 + 扩展新发现 1（闭包旧号码）——同属"拨错人"风险
8. E-3 双拨/穿透

**第二批 · 便捷性主线（下周）**
9. 主线 1 局域网直连：二选一决策（建议"清理误导文案+手机端隐藏 LAN_ONLY"走纯云，20 人规模云端完全够用；或恢复 LAN 但要重做鉴权）
10. A-2 切服务器自动重连、A-4 保活与无限退避、A-1 小米自动选卡开关
11. C-4 授权握手不误踢真机
12. C-1 REST/导出移入线程池（照抄 :818 写法）
13. 面板：凭据出 URL（POST+header）、401 停轮询、刷新不重置用户上下文

**第三批 · 打磨与运维（随后）**
14. C-5/C-6/C-8/C-9、C-7 竞态收尾、D-4 时区（先 `timedatectl` 确认服务器时区）
15. 面板遗留 8 项 + 新发现 8 条
16. 扩展 E-4~E-10 + 新发现 2/3/4/5
17. Android 新发现 1/2/3/5
18. Go 版：不发布则归档（G-1 不再成立）；要发布先补 dist + 队列/重连/粘滞三连
19. 安全收尾：改口令、9001/8090 收回公网、登录改 POST、allowBackup、日志打码、p12 出库

---

## 六、复核说明（不是问题的，避免误修）

1. PC 收到 ping 不回 pong 导致断开——不成立（服务端自动应答，Electron 不回 pong 是正确行为）。
2. websockets 未锁上界——当前不会启动失败，属防御性建议。
3. 扩展 11 位手机号识别——正常，不动。
4. Android 无障碍——仅部分小米机型需要，按"设置页开关"方案处理即可。
5. visit_records"以云端为准"覆盖——有意设计，只需补空值保护（A-3）。
6. 面板数据通道——11 个接口实测全部 200，空表是真实无数据而非故障。

---

*本报告所有条目可按文件:行号回溯；"线上正在发生"三条有生产日志与实测报告背书（见《验证报告-面板P0修复实测-2026-09-11.md》《验证报告-面板真实数据实测-2026-09-11.md》）。*

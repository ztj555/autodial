# 签名配置示例（复制为 keystore.properties 并填入真实值后使用）
# 也可通过环境变量 KEYSTORE_PASSWORD / KEY_PASSWORD / KEY_ALIAS / KEYSTORE_FILE 提供（env 优先）。
# 禁止把真实密码提交到仓库。详见部署指南。
#KEYSTORE_FILE=../autodial-release.p12
#KEY_ALIAS=autodial
#KEYSTORE_PASSWORD=your_store_password
#KEY_PASSWORD=your_key_password

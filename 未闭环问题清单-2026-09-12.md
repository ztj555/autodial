# 未闭环问题清单（2026-09-12 核查）

> ⚠️ **本文清单为 v4.22 基线（`1f962a5`）核查结果**。下表为 v4.23 修复批次（2026-09-12 当日完成，详见 `CHANGELOG.md`）后的**最新状态**；正文的"当前位置"行号是修复前的快照，已不代表现状。
> 结论：**43 条已闭环，24 条维持不修**（其中 9 条是 Go 端整端废弃；其余附理由）；另有个别条目部分闭环（见备注）。

## 闭环状态总表（2026-09-12 v4.23 全部批次完成后）

| 端 | 已闭环 | 维持不修（理由见 CHANGELOG「刻意维持不修」段） | 部分/其他 |
|---|---|---|---|
| 云端 | Y-1 Y-2 Y-3 Y-4 Y-7 Y-10 Y-11 Y-12 Y-13 Y-14 | Y-5（空 IP 限流——反代场景 429 风险）、Y-6（仅异常客户端可见）、Y-8（哈希升级有锁死管理员风险，建议换口令时顺带）、Y-9（仅 REST 审批链路） | — |
| 面板 | M-1 M-2 M-3 M-4 M-6（Esc；焦点陷阱未做） M-7 M-9 | M-5（纸面缺陷，实际不丢输入）、M-8 其余项（整洁度） | M-8 版本号子项已闭环（APP_VERSION 单一来源） |
| Android | A-2 A-4 A-5 A-6 A-7 A-8 A-9 A-10 A-11 A-13 A-14 | A-1（isConnected 门控语义变更）、A-3（补推触发点属生命周期重构，云端去重兜底）、A-12（HTTPS 属部署决策） | — |
| 扩展 | E-1 E-3 E-4 E-5 E-9 E-11 | E-2 E-6 E-7 E-8 E-10（iframe 状态机/遮挡可拖动绕开/主题双源/选择器写死——结构性或外观） | — |
| PC Electron | P-1 P-2 P-3 P-4 P-5 P-6 P-8 P-9 P-10 | P-7（经实测确认为误报：文件夹版 `getPath('exe')` 行为正确） | — |
| PC Go | — | G-1~G-9 全部（该端为废弃实验版，`go build` 不可用） | — |

**部署提醒**：云端修复（`cloud_relay_v2.py` + `dashboard.html`）需发到腾讯云才生效；Android 修复随 CI 产出的新 `app-release` 分发生效。

**档案说明（2026-09-12 文档治理）**：本清单的来源——`docs/archive/` 全部 11 份历史报告 + 根目录《综合复核报告-2026-09-11》《验证报告-v4.21综合复核修复》《场景化复核报告-2026-09-12》及人话版——已完成使命并删除；每条问题的核实结论与去向以本清单状态总表为准，修复详情见 `CHANGELOG.md`，需要报告原文可从 git 历史（`4a8e941` 之前）恢复。

---

> **这份清单怎么来的**：把 `docs/archive/` 下 11 份历史报告 + 根目录 2 份报告里列出的每一条问题，逐条回到当前源码核对（云端 / 面板 / 扩展 / Android / 两套 PC 端）。
> **已闭环**的条目不在这里（对应的历史报告可作废）；**仍然存在**的按端汇总如下，每条都标注了当前代码位置和来源编号，可直接按图索骥。
> 核查基准版本：`1f962a5`（v4.22 批次已合入）。

---

## 一、云端 `cloud-relay/python/cloud_relay_v2.py`

| # | 问题 | 当前位置 | 来源 |
|---|---|---|---|
| Y-1 | `calls/batch`、`events/log` **无鉴权**，也不校验 `device_id` 归属——可伪造他人通话记录 | :2348-2360, :2390 | 综合复核 3.1 / 4.2 |
| Y-2 | 单 PIN 免鉴权读全部来访数据（`?pin=` 即可） | :2658, :1878 | C-3 |
| Y-3 | `visits/batch`、`calls/batch` 仍用 **GET + query 传大 JSON**（反代下 414/截断，明文进访问日志） | :2348, :2463-2466 | C-6 |
| Y-4 | 来访去重仍是「先 SELECT 再 INSERT」非事务，竞态下仍可能重复 | :2588-2607 | C-7 |
| Y-5 | 限流**空 IP 直接放行**（`if not client_ip: return False`） | :644 | 云端新发现 2 |
| Y-6 | `/api/v1/visits` 传非法 `page`（如 `abc`）会静默回落到数组模式，响应结构突变 | :2662-2664, :2738-2742 | 云端新发现 4 |
| Y-7 | 登出/登录仍保留 **GET query 通道**（口令可进 URL 与日志） | :1927, :1933-1935 | 安全专项 |
| Y-8 | 口令哈希仍是**单轮 SHA-256 + 源码内硬编码盐** | :294-297 | 安全专项 |
| Y-9 | REST 审批路径转发用 `device_name` 冒充 `deviceId`（扩展实际走这条） | :2214 | 配对码 B2 残留 |
| Y-10 | 异步任务内仍有 `sys.exit(0)`（优雅退出已新增，但这处未改） | :3502-3508 | C-8 |
| Y-11 | 日志 / STATS 文件仍走 `APPDATA or ~`，Docker 重建即丢 | :64, :488, :508 | C-9 |
| Y-12 | `/api/status` 仍在协程内同步 `_connect_db()`（其余重查询已移线程池） | :1651-1662 | C-1 残留 |
| Y-13 | `test_stress_live.py` 仍**硬编码生产地址、无 `--dry-run`、无独立 DB** | `test_stress_live.py:43-49` | P0-C |
| Y-14 | `configure_firewall()` 无平台判断，任何平台都会调用 | :1280, :3342 | 面板 UX 8.2 |

## 二、管理面板 `cloud-relay/python/dashboard.html`

| # | 问题 | 当前位置 | 来源 |
|---|---|---|---|
| M-1 | `recent-clients` 渲染漏 `esc()`（pin/ip 未转义） | :1498 | D-9 |
| M-2 | 登出仍把 `?token=` 拼在 URL 上（其余已改 Authorization 头） | :1226 | 新发现 4 残留 |
| M-3 | `phoneHistoryCache` **永不过期**，历史行数据可能陈旧 | :1648, :1678 | 新发现 3 |
| M-4 | 防抖定时器跨页不清理（切页后仍可能触发） | :1558, :1784, :1847 | 新发现 6 |
| M-5 | `isBusyEditing` 手动「恢复」仍 `refreshAll(false)` 绕过编辑判定 | :2450 | 新发现 7 |
| M-6 | 弹窗**无 Esc 关闭、无焦点陷阱** | 全文件 | 验证报告 P2-12 |
| M-7 | 可读性：`.td-muted` 仍 11px、placeholder 带 `opacity:.7` | :279, :329 | 验证报告 P2-10 |
| M-8 | 首屏状态重复展示三处；系统日志页内嵌整套统计；上门登记筛选耦合；主题 10 套；版本号三处不一致 | :522/:566, :783-795, :2017/:2030, :945-956, :811 vs 云端 :1633 | 面板 UX 2.1/2.3/2.4/2.5/4 |
| M-9 | 通话记录页「设备筛选」下拉只在启动时加载一次 | :2499 | 面板 UX 3 |

## 三、Android `android-app/app/src/main/java/com/autodial/app/`

| # | 问题 | 当前位置 | 来源 |
|---|---|---|---|
| A-1 | 上报仍受 `isConnected` 门控——WS 未连上就不同步 | DialService.kt:848 | P0-B |
| A-2 | `READ_CALL_LOG` 缺失只打日志，**设置页无「去授权」入口** | DialService.kt:861-866 | P0-B |
| A-3 | 补推 `flushPendingSyncs` 仅在 WS 重连时触发，到不了 CONNECTED 就永久积压 | ConnectionManager.kt:953 | 新发现 5 |
| A-4 | `onCreate` 异常恢复分支仍漏 `startDataSync()` | DialService.kt:391-417 | 新发现 1 |
| A-5 | `simHandleCache` 永不失效——换卡/热插拔后拨错卡且无感知 | DialEngine.kt:34-42 | 新发现 3 |
| A-6 | 通话记录页「重拨」绕过 DialEngine（不走选卡、不写记录、不回执） | CallLogFragment.kt:344 | A-7 |
| A-7 | `onCreate` 无条件把 `manual_disconnect` 置 false，「手动断开」形同虚设 | MainActivity.kt:86-87 | A-5 |
| A-8 | 云端返回空数组仍整体覆写本地 `visit_records` | StatsFragment.kt:541-559 | A-3（管理员判为设计） |
| A-9 | 同步失败通用异常不给原因；电池优化引导每次启动都弹 | StatsFragment.kt:574, ConnectFragment.kt:912 | A-8 |
| A-10 | 防重复拨号仍只靠 `messageId`，无同号时间窗兜底 | DialService.kt:218 | A-9 |
| A-11 | 无障碍自动选卡仍**硬编码小米判断**，无设置开关 | DialEngine.kt:178 | A-1 |
| A-12 | 云同步走**明文 HTTP**，PIN 与客户数据经 URL 传输 | DialService.kt:980-981, AndroidManifest.xml:36 | AN-P1-3 |
| A-13 | 拨号路径在主线程查 `CallLog`（有卡顿风险） | DialEngine.kt:272-277 | AN-P1-6 |
| A-14 | 后台被杀后重连失效（无 WorkManager / AlarmManager） | 全仓无 | A-4 |

## 四、Chrome 扩展 `AutoDial-Extension/`

| # | 问题 | 当前位置 | 来源 |
|---|---|---|---|
| E-1 | 拨号链接 `__adHooked` 只钩一次，Click 闭包**固化旧号码**——SPA 换客户后可能拨错人 | content-script.js:1725-1732 | 新发现 1 |
| E-2 | 多 iframe「切页清零」互相误伤（一个帧切走会清掉别人的号码） | content-script.js:1792-1794 | 新发现 4 |
| E-3 | 「测试连接」`/health` 与 `uploadAdvisorName` 两处 fetch **无超时** | content-script.js:1273, background.js:232 | 新发现 5 / E-5 |
| E-4 | 浮窗点击无防抖，连点即双拨 | content-script.js:435-445 | E-3 |
| E-5 | 拨号结果只广播到 `frameId: 0`，iframe 内的浮窗收不到 | background.js:586 | E-4 |
| E-6 | 浮窗/挂断按钮固定定位 + 超大 z-index，可能遮挡页面元素 | content-script.js:359, :477 | E-6 |
| E-7 | 多 iframe 号码被「最后到达者」覆盖 | content-script.js:1722, background.js:619-621 | E-7 |
| E-8 | 主题状态双源（localStorage vs chrome.storage）不一致 | content-script.js:91 vs auth.js:12 | E-9 |
| E-9 | 离线队列 `queueCloudVisit` 无锁读改写（并发下可能丢） | background.js:421-427 | E-10 |
| E-10 | CRM 页面选择器写死（CRM 改版即失效） | content-script.js:28-29 | E-2 |
| E-11 | `escHtml` 不转义引号，登记弹窗存在属性注入 | content-script.js:1473-1475 | 4.2 |

## 五、PC 端 Electron `pc-app-Electron/`（**几乎整端未修**）

| # | 问题 | 当前位置 | 来源 |
|---|---|---|---|
| P-1 | `get-info` 不返回 `connected`/`firewall`，首屏状态与防火墙提示是死代码 | main.js:473-481 | P-2/P-3 |
| P-2 | 每次拨号**无条件写剪贴板**，覆盖用户已复制内容 | modules/server.js:169, :195 | P-4 |
| P-3 | **无单实例锁**，双开只弹错误框 | 全仓无 | P-5 |
| P-4 | 云重连满 30 次后**彻底停止**，夜间抖动白天不自愈 | modules/cloud.js:83-89 | P-6 |
| P-5 | 短信失败/超时不回 `sms-result`，窗口永久卡「等待手机确认」 | main.js:498-510 | 短信 |
| P-6 | 「关闭即退出」分支悬浮条仍存活 → 残留进程 | main.js:866-877, :927-931 | 退出 |
| P-7 | 便携版自启用临时解压路径，开机自启静默失效 | main.js:94-108 | 自启 |
| P-8 | 连接管理三连：`removeDevice` 无归属校验（旧连接杀新连接）、幽灵设备不清理、LAN hello 覆盖 `isCloud` | phone-connection-manager.js:264-297, :668-679, modules/server.js:351 | 连接管理 |
| P-9 | 设置**非原子写 + 静默失败**，损坏即回退默认并写回（配置全丢） | modules/settings.js:51-55, :60-77 | 设置 |
| P-10 | 日志**明文打印 PIN** | main.js:344 | 日志 |

## 六、PC 端 Go `pc-app-go/`（目录最后提交停在 8-23，报告后未再改）

| # | 问题 | 当前位置 | 来源 |
|---|---|---|---|
| G-1 | `//go:embed all:frontend/dist` 但该目录不存在 → **`go build` 直接失败** | main.go:13 | G-1 |
| G-2 | 云端 `phone_hello` 分支**不排空 `dialQueue`** → 唤醒手机后排队的号码永不拨出 | cloud.go:169-206 | 唤醒队列 |
| G-3 | 定时重连读 `cloudConnected` 必 false → **踢掉刚连上的服务器**，反复闪断 | cloud.go:296-306 | 定时重连踢人 |
| G-4 | Stale 设备不过滤，「手机已连接」假绿最长 120s | devices.go:21, :370 | 假在线 |
| G-5 | 同 PIN 抢连接：`removeDevice` 无条件清 `Ws/CloudWs` | devices.go:264-297 | 同 PIN 抢连接 |
| G-6 | `SetAutoStart` 前端零调用（死开关） | app.go:205 | SetAutoStart |
| G-7 | 设置写入 Roaming 根目录裸 `settings.json`，无 Electron→Go 迁移 | settings.go:73-77 | 设置路径 |
| G-8 | 数据竞争残留：`udp.go` 裸读 `pinCode`、`server.go` 裸读 `activePin`/`cloudConnected` | udp.go:50/65/91, server.go:71/103 | G-2~G-4 |
| G-9 | 日志明文 PIN | app.go:39, :406 | 日志 |

---

## 附：本次判定为「已闭环」的文档

| 文档 | 判定依据 | 处理 |
|---|---|---|
| `docs/archive/修复清单-面板第一批整改-2026-09-11.md` | 其 8 项改动**全部落实**（统一请求层、静默 catch、删假保存、服务端导出、表格滚动、`list|list` TypeError、搜索防抖、401 提示），且「未覆盖」5 条已被其他文档跟踪 | **已删除**（可 `git checkout HEAD -- <path>` 恢复） |

其余 11 份归档报告 + 根目录 2 份报告**均仍有未闭环条目**（即上表 60 余条），因此全部保留。
其中 `docs/archive/综合复核报告-实际使用场景-2026-09-11.md` 是**独立技术报告**（不是主报告的通俗版），含主报告未记录的条目（Android 14 前台服务权限、面板 toast XSS、短信链路、Electron/Go 若干条），**不可删**。

# 待验证问题清单

> 每次构建/部署后按此清单逐项验证，通过打 ✅，失败打 ❌。

---

## v4.14 新增待验证项（2026-08-19）

### A1. 扩展端设备授权流程

**描述**：v4.14 修复了云中继 `auth/respond` 的 NameError 并增加归属校验（需携带与请求一致的 `pin`）。扩展端授权弹窗（auth.html）已同步携带 PIN。

**验证步骤**：
1. 管理员在云中继面板为设备 A 预设默认 PIN
2. 设备 A 使用不同 PIN 登录 → 收到 `auth_pending`
3. CRM 页面打开（扩展在线）→ 5 秒内弹出授权窗口 → 点「允许」
4. 验证手机收到 `auth_ok`（此前该路径永不返回 `auth_ok`）

**状态**：⏳ 待验证

### A2. 管理员敏感端点鉴权

**描述**：`/api/v1/pins`、`/api/v1/devices`、`/api/status` 等读端点现需管理员令牌；`/api/v1/visits` 无 pin 时同样需要；手机端 `?pin=` 同步不受影响。

**验证步骤**：
1. 未登录访问 `/api/v1/pins` → 401 `UNAUTHORIZED`
2. 登录后携带 `?token=` 访问 → 200
3. 手机端「统计→同步」仍正常（走 `?pin=`）

**状态**：⏳ 待验证

### A3. PC 端本地端口来源校验

**描述**：35432 端口仅接受回环 Host + 可信来源；外部网页/DNS rebinding 将收到 403；扩展拨号不受影响。

**验证步骤**：
1. 扩展点号码拨号（chrome-extension 来源）→ 正常
2. 浏览器直接打开 `http://127.0.0.1:35432/` → 正常（无来源请求放行）
3. 从任意网页 fetch `http://127.0.0.1:35432/dial?number=...` → 403

**状态**：⏳ 待验证

---

## 手机端

### 1. 录上门后统计详情弹窗

**描述**：在「录上门」页面成功登记一条客户（填写称呼、手机号、选择顾问、点击完成登记），然后切换到「统计」页，点击上门统计卡片中的数字（如「今日」或「本周」），应弹出窗口显示该时段内的客户姓名、手机号和登记时间。

**预期**：弹出 BottomSheetDialog（底部弹窗），显示如：
```
今日上门（1条）
1. 张三  13800138000
   07-04 15:30
```

**验证步骤**：
1. 打开 App → 录上门 → 完成一次登记（确保显示"✅登记成功"）
2. 切换到「统计」Tab
3. 确认「上门统计」卡片中「今日」数字为 1 或更大
4. 点击「今日」数字
5. 检查弹窗内容

**状态**：⏳ 待验证

**关联修复**（2026-07-04）：
- `RegisterFragment.saveVisitRecord()` — 登记成功时写入 `visit_records` JSON 到 SharedPreferences
- `StatsFragment.loadVisitStats()` — 读取 `visit_records` 并填充 `visitRecords` 列表
- `StatsFragment.showVisitDetail()` — 弹窗显示过滤后的上门记录
- 两处 JSON 解析的静默 catch 已改为带日志的错误处理
- 调试命令：`adb logcat -s AutoDial:D StatsFragment:D`

---

### 2. 云端同步上门数据

**描述**：在统计页点击「🔄 同步」按钮，从云中继拉取上门数据。

**预期**：
- 云端可达 → Toast "✅ 同步完成，共 X 条上门记录" 或 "📋 云端暂无上门记录"
- 云端不可达 → Toast "❌ 同步失败，请检查云端连接"

**状态**：⏳ 待验证

---

### 3. 顾问姓名选择器

**描述**：录上门页面的「接待顾问姓名」字段，点击应弹出 CRM 顾问列表供选择。

**预期**：弹出 BottomSheetDialog（底部弹窗）"选择接待顾问"，列出所有顾问姓名（如韩俊、金晟...），选中后自动填入。

**状态**：⏳ 待验证

---

## 插件端

### 4. 同步登记列表按钮

**描述**：点击 popup 中「🔄 同步登记列表」按钮。

**预期**：
- 不在 CRM 页面 → 自动打开/跳转到 `list_user_visit.html`
- 已在列表页 → 抓取表格数据同步到云端
- 无管理员限制：content-script.js 已去掉主管校验，任何坐席均可同步（不再调用 checkIsAdmin）

**状态**：⏳ 待验证

---

## 云端

### 5. CORS 头与鉴权

**描述**：访问 `http://101.34.65.254:35430`（cloud_relay_v2.py Web 管理面板）时，API 请求不被浏览器拦截。v4.14 起敏感读端点需管理员登录（携带会话令牌）后访问，未登录时相关接口返回 401。

**状态**：⏳ 待验证

---

### 6. CRM 不再双重同步

**描述**：插件/手机端登记后，CRM 系统只收到一条登记记录（不再重复）。

**状态**：⏳ 待验证

---

> 最后更新：2026-08-01

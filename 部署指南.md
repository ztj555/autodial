# AutoDial 部署指南

> 腾讯云 101.34.65.254 | Ubuntu 22.04 | 1Panel v2.0.15 | 最后更新：2026-08-19

## 一、云中继部署

### 环境要求
- Ubuntu 22.04+
- Python 3.10+
- pip: `websockets pystray Pillow`（生产可仅装 `websockets aiohttp`）

### Docker 部署（推荐）

```bash
docker run -d --restart=always --name autodial-relay \
  -p 35430:35430 \
  -v /opt/autodial/data:/app/data \
  autodial/cloud-relay
```

> **数据持久化说明（v4.14 起）**：容器内启动脚本（entrypoint.py）默认设置 `AUTODIAL_DB_PATH=/app/data/visits.db`，数据库与日志（APPDATA=/app/data）均落在挂载卷 `/app/data` 内，容器重建/升级不会丢失数据。若需自定义数据库位置，可在 docker 命令中注入环境变量覆盖：
>
> ```bash
> docker run -d ... -e AUTODIAL_DB_PATH=/app/data/visits.db autodial/cloud-relay
> ```

### Supervisor 部署

```bash
# 安装依赖
cd /opt/autodial/cloud-relay/python
pip install websockets pystray Pillow

# Supervisor 配置 /etc/supervisor/conf.d/autodial.conf
[program:autodial-relay]
command=/usr/bin/python3 /opt/autodial/cloud-relay/python/cloud_relay_v2.py
directory=/opt/autodial/cloud-relay/python
autostart=true
autorestart=true
user=root
stdout_logfile=/var/log/autodial-relay.log
stderr_logfile=/var/log/autodial-relay-err.log

# 启动
supervisorctl reread && supervisorctl update && supervisorctl start autodial-relay
```

### 验证
```bash
curl http://localhost:35430/health
# 预期: {"service": "AutoDial Cloud Relay", "version": "4.10", "port": 35430, "uptime_seconds": ..., "total_connections": ..., "total_groups": ...}
```

## 安全配置（v4.14 起）

- 管理员默认账号 `18335162275` / 初始密码 `123456`（数据库中以 SHA-256 加盐哈希存储），**首次登录后请立即修改**；登录接口已限频（60 秒内失败超 5 次返回 `RATE_LIMITED`）
- 除 `/health`、`/`、`/api/v1/dial`、`/api/v1/visit` 等业务端点外，**管理/统计/客户数据读端点均需管理员令牌**（`?token=` 或 `Authorization: Bearer`）
- 手机端上报端点（`calls/batch`、`events/log`、`stats/report`）为设备自身上报，无需管理员令牌

## 二、双实例部署（35430 + 35440）

1Panel 运行两个容器实例，或 Supervisor 运行两个进程（不同端口）：
```bash
python cloud_relay_v2.py --port 35430 &
python cloud_relay_v2.py --port 35440 &
```

## 三、Android APK 构建

GitHub Actions 自动构建，产物包括：
- `app-release` — 正式签名 APK
- `app-debug` — Debug 签名 APK

本地构建：
```bash
cd android-app
./gradlew assembleRelease  # 需密钥
./gradlew assembleDebug    # 无需密钥
```

### 签名密钥
- 文件：`android-app/autodial-release.p12`
- 算法：RSA 2048, SHA256
- 有效期：25 年
- Secrets 配置：`KEYSTORE_BASE64` 放在 GitHub Actions Secrets

## 四、Chrome 扩展分发

1. 加载已解压的：`chrome://extensions/` → 开发者模式 → 加载 `AutoDial-Extension/`
2. 打包 .crx：扩展管理页 → 打包扩展 → 选择目录
3. 私钥文件 `.pem` 妥善保管，用于后续版本更新

## 五、服务器列表管理

### 默认服务器
首次启动自动使用 `101.34.65.254:35430`，可在 App/插件端修改。

### 远程列表
- GitHub Gist: `https://gist.githubusercontent.com/ztj555/cb6a6bb0ddbe3d4e651d5bb3411777d5/raw/AutoDialservers.txt`
- Gitee: `https://gitee.com/zuo-tingjun/AutoDialserverslist/raw/master/servers.txt`

格式：
```
[old]
262ao85kz470.vicp.fun:55535 旧主站
[new]
101.34.65.254:35430 腾讯云主站
```

支持行末别名，四端自动兼容。

## 六、运维脚本

| 脚本 | 用途 |
|------|------|
| `/opt/autodial/scripts/restart.sh` | 重启云中继 |
| `/opt/autodial/scripts/logs.sh` | 查看最近日志 |
| `/opt/autodial/scripts/status.sh` | 健康检查 |
| `/opt/autodial/scripts/backup.sh` | 备份 visits.db |

## 七、防火墙

```bash
# 开放 35430 端口
ufw allow 35430/tcp

# 如用腾讯云安全组，在控制台添加入站规则
```

## 八、监控要点

- `/health` 端点监控（UptimeRobot 等）
- SQLite `visits.db` 定期备份
- 日志文件 `/var/log/autodial-relay.log` 轮转（已内置 RotatingFileHandler）

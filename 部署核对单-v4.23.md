# AutoDial v4.23 部署核对单（2026-09-12）

> 修复全系列已完成、CI 全绿。本文是你手上仅剩的两件事的操作清单，照着做即可。
> 代码侧所有验证已完成：py_compile / node --check / pytest 109 通过 / 并发去重实测 / 冒烟实测，无需再验。

---

## 一、云端部署（腾讯云，约 5 分钟）

### 要发的 2 个文件

| 文件 | 本地路径 | MD5（发完务必核对） | 行数 |
|---|---|---|---|
| `cloud_relay_v2.py` | `cloud-relay/python/cloud_relay_v2.py` | `1bb90de03c9ad5593585b1885c00caf5` | 3753 |
| `dashboard.html` | `cloud-relay/python/dashboard.html` | `fc0d0b7ca8ed9473ae85e72283ddd023` | 2591 |

### 步骤

1. **上传**两个文件到服务器原路径（覆盖前可先 `cp xxx xxx.bak-v422` 留个后路）
2. **核对 MD5**：服务器上 `md5sum cloud_relay_v2.py dashboard.html`，与上表一致才算传对
3. **语法预检**：`python3 -m py_compile cloud_relay_v2.py`，无输出即通过；报错就不要重启，回滚备份
4. **重启**：`supervisorctl restart autodial`（按你原来的服务名）
5. **上线验证**（3 条命令或浏览器点开即可）：
   - `curl http://127.0.0.1:35430/health` → `"version": "4.23"` ← **看到 4.23 就说明新代码生效了**
   - 面板登录后 → 首页能正常打开、"系统信息"页的**服务版本显示 4.23**（不再是 6.0）
   - 手机/扩展正常登记一条来访 → 面板能看到，即业务链路正常

### 这次云端都修了什么（人话）

- **别人能偷看客户数据**：填个随便的 PIN 就能拉走整组客户姓名/手机号 → 堵上了
- **别人能伪造上报**：随便编个设备号就能写假通话记录 → 设备必须先注册过才收
- **口令不出现在网址里**：登录/登出/上报都改走请求头（老版本 App 短期共存也兼容）
- **并发登记重复入库**：两个请求同时到只记一条（实测验证）
- **数据库卡顿不再拖死全部连接**、Docker 重建不再丢日志、Linux 上不再白跑防火墙命令、退出不再残留托盘僵尸
- 面板：弹窗支持 Esc、筛选设备列表自动刷新、缓存 60 秒过期、小字变大、登出不带 token、版本号显示真实的 4.23

## 二、APK 分发（手机端）

1. 打开 GitHub 仓库 → Actions → 最新一次绿色构建（run `34699099455` 之后的任意一次）→ Artifacts → 下载 **`app-release`**
2. 分发给同事安装（签名版，可直接覆盖安装，数据不丢）
3. 装完后**不需要任何新配置**，老 PIN 直接用

### 这次手机端都修了什么（人话）

- **后台被杀会自己活过来**：15 分钟一次自动检查，断了自动重连
- **不会拨错卡/拨错人**：换卡自动重识别、同一号码 4 秒内不会拨两次、从通话记录页拨号也走正规链路
- **手动断开真的管用**：点了断开就不会偷偷重连
- **同步更安全**：失败会告诉你原因；云端返回空列表不再清掉手机上的本地记录
- **骚扰减少**：电池优化弹窗最多 7 天提醒一次；权限被拒了会在统计页给"去授权"按钮

## 三、Chrome 扩展（内部分发后）

在 `chrome://extensions` 点一下**重新加载**即可生效。本次修复：点击拨打读实时号码（SPA 换客户不拨错）、iframe 浮窗也能收到结果、离线队列不丢数据、测试连接有超时。

## 四、刻意不修的（免得你疑惑为什么没动）

详见《未闭环问题清单-2026-09-12.md》顶部状态总表。要点：口令哈希升级（怕锁死管理员，建议下次换口令时一起做）、HTTPS（需要你给服务器配证书）、Go 版 PC 端（废弃不修）。

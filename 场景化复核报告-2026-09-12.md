# AutoDial 场景化复核报告（2026-09-12）

> **场景设定**：公司内部小范围使用，约 20 人。客服/坐席端 = Android 手机 + Chrome 扩展；管理端 = 云端面板，单人管理。
> **判断取向**：① 优先解决日常使用中真能碰到的问题；② 安全以"够用、且不给日常操作加步骤"为准；③ 不为理论风险牺牲便捷性。
> **核查基线**：源码 `1f962a5`（v4.22 批次已合入）。
> **输入**：上一版《未闭环问题清单-2026-09-12.md》共 67 条。
> **本文性质**：**纯只读复核，未改动任何代码**。所有结论均回到源码逐条核对，不成立的已剔除，废弃端整体归档。

---

## 零、结论速览

67 条收紧为 4 档：

| 处理建议 | 条数 | 判断标准 |
|---|---|---|
| **P0 立即修** | 7 | 天天可能碰到，或直接违背"云端只有我能访问" |
| **P1 建议修** | 15 | 条件触发，但一触发就丢数据 / 拨错人 / 卡死 |
| **P2 顺手修** | 20 | 日常基本无感，改动成本极低 |
| **归档 / 不修** | 25 | 废弃端（Go 全端 9 条）、误报、当前部署形态下不成立 |

**分布**：真正值得动手的是前 22 条（P0+P1）。其中 PC 端 Electron 占 5 条，是密度最高的地方——因为它承载短信/拨号主流程，却在上一轮修复里基本没被碰过。

### 必须先在报告开头纠正的一个前提

你在需求里说"云端是我一个人保存、不允许别人随意访问"。但代码与部署记录显示，**当前实际状态是：整个云端服务对全网开放**——

| 事实 | 证据 |
|---|---|
| 监听地址写死全网卡 | `cloud_relay_v2.py:3349` `async with serve(handle_connection, '0.0.0.0', PORT, ...)` |
| 端口 35430，无任何 TLS | 全文件搜不到 `ssl` / `SSLContext` / `certfile`；`README.md:469` 明确把「HTTPS 迁移」列为**遗留未做项** |
| 生产机是公网 IP，防火墙全网放行 | `README.md:357`「腾讯云 101.34.65.254」；`:455` `ufw allow 35430/tcp`（另加腾讯云安全组入站规则） |
| 管理面板本身就是明文 HTTP | 面板地址即 `http://101.34.65.254:35430/`，仓库内无 nginx/反代配置 |
| 手机端与云端的链路同样明文 | `AndroidManifest.xml:36` `usesCleartextTraffic="true"` |

**一句话**：现在不是"只有 20 个人知道地址"就能挡住的状态——互联网上的端口扫描器不认人，扫到 35430 就能看到你的登录页。这一层事实直接把下面两条安全项推到了 P0。

**好消息**：如果客服只在固定网络（公司办公网 / 固定出口 IP）里使用，可以在腾讯云安全组把 35430 限制为**只允许公司出口 IP 访问**——零体验代价，一次性砍掉绝大部分公网风险。是否需要这么做，取决于"客服会不会在外面用 4G/5G 流量拨号"——这一点请你确认。

---

## 一、P0 立即修（7 条）

| # | 端 | 问题 | 为什么在你们场景下最要紧 | 证据位置 |
|---|---|---|---|---|
| 1 | 云端 | **越权读整组客户数据**：`/api/v1/visits?pin=任意值&group=1` 能免登录读出该分组**全部**客服的来访记录（客户姓名/手机/事由）。根因是鉴权只在"pin 为空"时校验，而 `group` 分支优先级高于 `pin` 分支 | 直接推翻"云端只有我能访问"。攻击门槛极低：不需要口令、pin 随便填、group 是小整数。且服务在公网 | `cloud_relay_v2.py:2658-2660`（pin 非空即跳过管理员校验）、`:2679-2693`（group 分支优先于 pin 分支） |
| 2 | 云端 | **数据上报接口无鉴权**：`/api/v1/calls/batch`、`/api/v1/events/log` 不校验任何凭据，也不校验 `device_id` 归属 | 公网可达 → 任何人可批量灌入伪造通话记录、伪造设备、污染统计报表，让"拨打量/接通量"失真 | `:2349-2388`（calls/batch）、`:2391-2418`（events/log），均无 `_check_admin` |
| 3 | PC Electron | **短信窗口永久卡死**：发送后若手机没回执（短信被取消、手机中途离线、ACK 超时），代码只写日志、**从不回 `sms-result`**，前端永远停在"等待手机确认…"，按钮不可再点 | 触发条件是**常态而非边缘**。用户只能用"关窗重开"自救，且不知道为什么要重开 | `main.js:498-510`（超时仅 `fileLog`）；前端 `renderer/sms.html:568-580` 只认 `sms-result` 复位 |
| 4 | PC Electron | **配置静默全丢**：`settings.json` 非原子写入且吞掉异常；一旦写坏，下次启动**静默回退默认值并写回**，配对码/云服务器/手机备注/主题/自启全部清空，用户得**重新配对** | 数据丢失级，且**没有任何提示**。断电、强制关机、崩溃都可能触发 | `modules/settings.js:50-54`（`try{writeFileSync}catch{}`）、`:40-47`（解析失败回默认）、`:76`（无条件 `saveSettings` 写回） |
| 5 | PC Electron | **云重连累计约 1 小时后永久停止**：阶梯退避累计失败到上限即"停止自动重连，等待手动触发"，此后网络恢复也不会自愈 | 夜间断网 / 云端维护后，第二天表现为"手机连不上 PC、拨号没反应"，必须重启程序 | `modules/cloud.js:83-87`（`MAX_CLOUD_RETRY = 30` 后 `return`） |
| 6 | PC Electron | **同 PIN 重连顶掉新连接**：旧连接的关闭回调会把这个 **新** 连接的 `ws` 置空，并 `stale = true`——表现为"手机明明显示在线，就是拨不出去" | 手机切网重连（WiFi↔4G）就会触发。同一处还连带：幽灵设备永不清理、云通道状态被冲成 false | `phone-connection-manager.js:264-305`（不校验 ws 归属）、`modules/server.js:335-339`（先关旧再注册）、`:668-679`（stale 设备不被清理） |
| 7 | Android | **后台被杀后连接不自恢复**：全工程无 `WorkManager`/`AlarmManager`/`JobScheduler`，亮屏广播是**动态注册**（进程没了就失效），只剩开机自启一条路 | 国产 ROM 杀后台是常态。客服以为还在线、实际已掉线，拨号无响应且无人察觉 | 全仓无调度组件；`DialService.kt:611-631` 动态注册亮屏广播；仅 `AndroidManifest.xml:63-70` 的 `BootReceiver` 兜底 |

---

## 二、P1 建议修（15 条）

### Android（4 条）

| # | 问题 | 触发条件 | 证据 |
|---|---|---|---|
| A-7 | "手动断开"不跨重启：`onCreate` 无条件把 `manual_disconnect` 置 false，重开 App 后自动重连复活 | 用户手点断开 → 重开 App | `MainActivity.kt:86-87` |
| A-2 | 通话记录权限被拒后无"去授权"入口：只写日志，设置页无按钮，且记录**静默不上传** | 用户误点"拒绝" | `DialService.kt:861-866`；`fragment_call_log.xml:292-300` 只有文案无按钮 |
| A-6 | 通话记录页"重拨"绕过选卡引擎：不经 DialEngine，不按规则选卡、不写记录、不回执 PC，也不进统计 | 客服在通话记录里点重拨 | `CallLogFragment.kt:344`、`CallDetailSheet.kt:94` 直接 `Intent.ACTION_CALL` |
| A-8 | 云端返回空数组时整体覆写本地 `visit_records`，**清空本地上门记录** | 云端数据被清 / 接口异常返空，管理员点"同步" | `StatsFragment.kt:541-559`（未判空直接覆写） |

### Chrome 扩展（3 条）

| # | 问题 | 触发条件 | 证据 |
|---|---|---|---|
| E-1 | 拨号链接只钩一次，Click 闭包**固化旧号码**——SPA 换客户后可能**拨错人** | CRM 复用同一个 `<a>` 元素换号码（不重建 DOM） | `content-script.js:1725-1732` |
| E-4 | 浮窗点击无防抖、无在途锁，**连点两次就拨两通** | 客服手快 / 习惯性双击 | `content-script.js:435-446`；`background.js:246+` 拨号侧也无去重 |
| E-3 | "测试连接" `/health` 请求**无超时**，服务器黑洞时按钮永久卡在"测试中…" | 云端不可达（非拒绝，而是超时） | `content-script.js:1273`（同文件 :1298 有 8s 超时，此处漏了）、`background.js:232` |

### PC Electron（2 条）

| # | 问题 | 触发条件 | 证据 |
|---|---|---|---|
| P-6 | 选「关闭即退出」后**进程不退出、悬浮条仍在桌面**，托盘图标却已消失 | 用户主动改过关闭行为设置 | `main.js:866-876`（只 `tray.destroy()`）、`:927-931`（`window-all-closed` 因悬浮条存活而永不触发） |
| P-2 | 每次拨号**无条件覆盖系统剪贴板** | 坐席先复制了别的内容再拨号 | `modules/server.js:169`、`:195` |

### 安全（P1 里与"只有我能访问"直接相关的 4 条）

| # | 端 | 问题 | 说明 | 证据 |
|---|---|---|---|---|
| A-12 | Android | 云同步走**明文 HTTP**，PIN 与客户数据经 URL 传输 | 公网链路上可被嗅探；属"够用即可"范畴但需与云端一起解决 | `DialService.kt:979-982`；`AndroidManifest.xml:36` |
| Y-7 | 云端 | 登录/登出仍保留 **GET query 通道**，口令与 token 可进 URL、浏览器历史、访问日志 | 面板侧登录已改 POST，但服务端通道未关；登出仍拼 `?token=` | `:1929-1935`（登录仍读 query）、`:1984-1989`（登出） |
| Y-3 | 云端 | `calls/batch` 仍用 **GET + query 传大 JSON**，明文进访问日志，反代下易 414/截断 | `visits/batch` 已支持 POST body，`calls/batch` 没跟上 | `:2348-2353` |
| Y-13 | 云端 | 压测脚本硬编码生产地址、无 `--dry-run`、无独立库，且**不清理测试数据** | 运维风险：误跑一次就把合成 PIN/设备/来访写进生产库 | `test_stress_live.py:43-46`；全文件无 DELETE |

### 面板（2 条）

| # | 问题 | 触发条件 | 证据 |
|---|---|---|---|
| M-1 | `recent-clients` 渲染漏 `esc()`（pin/ip 未转义），同块其它字段已转义 | 需要外部设备能连上公网 35430 并伪造 pin | `dashboard.html:1498` |
| M-9 | 通话记录页「设备筛选」下拉只在启动时加载一次 | 新设备注册后不重启面板就选不到 | `dashboard.html:2499` |

---

## 三、P2 顺手修（20 条）

用户基本无感，但改动成本很低，做其它改动时顺手带上。

| 端 | 条目 |
|---|---|
| 云端 | Y-4（来访去重非事务，并发下可能重复；crm_id 路径已有唯一索引兜底）、Y-5（限流对空 IP 直接放行）、Y-8（口令哈希单轮 SHA-256 + 源码硬编码盐 `autodial#v4`）、Y-12（`/api/status` 仍在协程内同步查库，数据量大后周期性卡顿） |
| 面板 | M-2（登出仍拼 `?token=`）、M-3（手机历史 PIN 缓存永不过期）、M-4（防抖定时器跨页不清理，仅多发一个隐藏请求）、M-6（弹窗无 Esc、无焦点陷阱） |
| Android | A-1（WS 断线期间记录延迟上传，重连后补齐）、A-3（补推仅 WS 重连触发）、A-4（`onCreate` 异常恢复分支漏 `startDataSync`）、A-9（同步失败不给原因；电池优化引导每次弹）、A-10（防重复拨号只靠 `messageId`）、A-13（拨号路径在主线程查 CallLog，极轻微） |
| 扩展 | E-2（多 iframe 切页互相误伤）、E-6（浮窗固定定位可能压住页面按钮，可拖动绕开）、E-7（多 iframe 号码被后者覆盖）、E-8（主题双源 localStorage vs chrome.storage，纯外观）、E-9（离线队列读改写无锁） |
| PC | P-3（无单实例锁，双开弹"错误"框但会退出）、P-10（日志明文打印 PIN，需日志文件外泄才有风险） |

---

## 四、归档 / 不修（25 条）

### 1. `pc-app-go/` 整端归档 —— 9 条

**判定依据**（三重，均可复现）：

- **构建阻断**：`main.go:13` `//go:embed all:frontend/dist`，但 `frontend/dist` 被 `.gitignore` 屏蔽且实际不存在 → 实测 `go build` 直接报 `pattern all:frontend/dist: no matching files found`。**任何人都编译不出这个版本**。
- **已拍板不动**：`CHANGELOG.md:142` 明确写了「14 Go 版不动」。
- **文档反复确认**：`docs/archive/代码审查报告-实际使用问题-2026-09-11.md:121, :319`、`综合复核报告-人话版-2026-09-11.md:67` 三处均写「当前 PC 端主力为 Electron」。
- **无有效产物**：仓库里唯一的 `autodial-pc.exe` 是 8-22 的本地产物，其中不含当前前端源码的任何特征串，已对不上源码。

**结论**：G-1~G-9 全部**不需要修**，建议直接在 README 里标注"实验版 / 未维护"，或整目录移入 `docs/archive/`。

### 2. 核查后不成立 —— 2 条

| 原编号 | 原描述 | 核查结论 |
|---|---|---|
| E-5 | 拨号结果只广播到 `frameId: 0`，iframe 内浮窗收不到 | **不成立**。浮窗与挂断按钮**只在顶层创建**（`content-script.js:344` `if (isTopFrame)` 块内定义 :350 / :466，调用点 :1588-1589 同块）。iframe 内本就没有浮窗，`frameId:0` 正好命中唯一浮窗 |
| Y-11 | 日志/STATS 走 `APPDATA or ~`，Docker 重建即丢 | **按官方部署方式不成立**。`Dockerfile:38` 已 `os.environ["APPDATA"] = "/app/data"` 挂载卷；README:377 有说明。仅当不用官方 entrypoint 裸跑容器才会丢——降到"代码层缺独立路径变量"的整洁性问题 |

### 3. 场景下低价值 —— 14 条

| 条目 | 为什么不建议现在花时间 |
|---|---|
| A-5 | `simHandleCache` 永不失效导致换卡拨错卡。20 人基本不换卡，且换卡后会重启 App —— 理论问题 |
| A-11 | 无障碍自动选卡硬编码小米。若内部机型统一，改了也没收益；先确认机型分布 |
| M-5 | `isBusyEditing` 手动恢复的纸面缺陷：点徽标本身就把焦点移出输入框，实际几乎不会丢输入 |
| M-7 | `.td-muted` 11px、placeholder `opacity:.7` —— 纯可读性微调，不影响任何操作 |
| M-8 | 首屏状态重复三处、日志页内嵌统计、主题 10 套、版本号不一致 —— 整洁度问题。唯一值得顺手改的是 `dashboard.html:811` 把"设计系统版本 6.0"当"服务版本"展示（排查问题时会误判线上版本） |
| E-10 | CRM 选择器写死，但 :41-56 有 TreeWalker 兜底，且非精确结果只提示不自动切 PIN —— 风险已被削弱 |
| E-11 | `escHtml` 不转义引号 → 属性注入。需姓名含 `"`，中文姓名几乎不会出现 —— 理论问题 |
| P-1 | `get-info` 不返回 `connected`：主窗口启动时本就无手机在线，随后 `status-update` 会补上，无感。**但同一条里 `firewall` 提示丢失值得修**（见下方备注） |
| P-7 | 便携版自启用 `%TEMP%` 解压路径 → 自启静默失效。**仅当分发的是 portable 包**；若实际发的是文件夹版（`pack.js`）则不受影响 —— 请确认你们实际发哪种包 |
| P-10 | 日志明文 PIN：需日志文件外泄才有风险。20 人内部环境风险低，但改一行日志成本极低，可顺手 |
| Y-6 | 非法 `page=abc` 回落到数组模式：面板永远传数字，只有手工/异常客户端会看到 |
| Y-9 | REST 审批用 `device_name` 冒充 `deviceId`：仅走 REST 审批链路的设备会错配，特定链路才遇 |
| Y-10 | `shutdown()` 内 `sys.exit(0)`：生产是 headless/Supervisor 托管，走 SIGTERM 不受影响；只有 Windows 托盘模式点退出才可能不干净 |
| Y-14 | `configure_firewall()` 无平台判断：Linux 上 `netsh` 抛异常但被吞掉，只留一行日志噪音，功能不受影响 |

---

## 五、与上一版清单的修正

本轮复核修正了上一版 5 处判断：

1. **新增一条 P0**：Y-2 原本写作"单 PIN 免鉴权读**全部**来访数据"，表述不准确——单纯 `?pin=xxx` 只返回该 PIN 自己的记录（手机端同步的正常接口）。但复核时发现**真正的越权路径**：`?pin=任意非空值&group=N` 因分支优先级绕过鉴权，可读**整组**数据。这条比原描述更严重，已升为 P0 第 1 条。
2. **剔除 1 条误报**：E-5 不成立。
3. **修正 1 条夸大**：Y-11 在官方 Docker 部署下不成立。
4. **Go 端 9 条整端归档**：原清单按"未修"计入，现判定为废弃端，不计入待办。
5. **修正 PC 端描述**：原写"报告后几乎未动"，实际 v4.22 改过 `main.js`/`modules/server.js`（监听改 `0.0.0.0`、未捕获异常不再退出、HTTP 拨号 5 秒去重）；但 P-1~P-10 这 10 条确实仍未修。

---

## 六、建议的动作顺序

### 第一批（本周，6 项）—— 体验杀手 + 越权口子

1. **Y-2** 越权读整组（安全，且门槛最低）
2. **P-5** 短信窗口永久卡死（PC 主流程，常态触发）
3. **P-9** 配置静默全丢（数据丢失）
4. **A-14** 后台被杀不自恢复（Android 日常掉线）
5. **P-8** 同 PIN 重连顶掉新连接（"在线却拨不出去"）
6. **P-4** 云重连 1 小时后停止（夜间故障不自愈）

### 第二批 —— 数据完整与误拨

**Y-1**（上报接口无鉴权）、**A-8**（返空覆写本地记录）、**E-1**（固化旧号码拨错人）、**A-6**、**A-7**、**A-2**、**P-6**

### 第三批 —— 安全收口（配合"只有我能访问"）

**Y-7**、**Y-3**、**A-12**、**M-1**、**Y-13**

### 顺手带上（做别的改动时一起）

P2 那 20 条。

---

## 七、关于"云端只有我能访问"—— 三档做法

按投入从低到高，你可以任选其一或组合：

| 档位 | 做法 | 成本 | 效果 |
|---|---|---|---|
| **零成本（最推荐）** | 腾讯云安全组把 35430 入站规则从"全网"改成**只允许公司出口 IP + 你的常用 IP** | 改几条规则，5 分钟 | 公网扫描器直接扫不到端口，一次性解决 P0-1/P0-2 的"公网可达"前提。**前提：客服不会在外面用手机流量拨号** |
| **低成本（代码）** | 修 Y-2 越权 + 关掉 Y-7 的 GET 口令通道 + 给 Y-1 加校验 | 半天 | 即使被扫到，也读不到数据、灌不进假数据 |
| **中成本（架构）** | 用 1Panel 自带 openresty 或 Caddy 反代 35430，加 TLS 证书（Let's Encrypt 免费） | 1~2 天，含证书续期 | 口令与客户数据不再明文过公网；但要处理手机端 `usesCleartextTraffic` 与证书信任 |

> **我的建议**：先做「零成本」那一档。20 人内部使用、且你自己保管服务器，安全组限 IP 是投入产出比最高的一步，而且**完全不增加任何人的操作步骤**——这正符合你说的"不能太增加体验的复杂程度"。代码侧则只修 Y-2 这一个越权口子即可。

---

## 附：核查方法说明

- 每条结论均由子代理回到源码用 grep/read 定位**当前**行号后核对，不接受"行号漂移"导致的误判。
- 三个并行核查分别覆盖：Android+扩展 / PC 两端 / 面板+云端（含网络暴露面专项）。
- 关键前提（`0.0.0.0` 监听、无 TLS、`ufw` 放行）由主代理亲自二次验证。
- 全程只读，未修改任何代码文件。

# 这份报告在说什么（人话版）

> 这是《场景化复核报告-2026-09-12》的通俗翻译。
> 前提：我是**只看了代码、没有改任何东西**。你看完觉得哪条该修，再告诉我。

---

## 一句话总结

上一轮翻出来的 67 个问题，我按"你们公司 20 个人实际用起来会不会被坑"重新过了一遍：

- **有 7 个是真该赶紧修的**（天天能碰到，或者你的服务器现在是敞开的）
- **15 个是碰到就难受的**（比如丢了数据、拨错人）
- **20 个是顺手就能改的**（用户根本感觉不到）
- **25 个直接不用管**（其中 9 个是那套本来就废弃的 Go 版电脑端，连打包都打不出来）

真正要花时间的就前 22 个。

---

## 先说一件你必须知道的事：**你的服务器现在是"敞着门"的**

你说"云端是我一个人保存、不让别人随便访问"。但代码和部署记录摆在一起看，**实际不是这样**：

- 服务监听的是 `0.0.0.0`——意思是**谁都能连，不限来源**（代码 `cloud_relay_v2.py:3349`）
- 你的服务器是腾讯云的公网机器 `101.34.65.254`，防火墙命令写的是 `ufw allow 35430/tcp`——**对所有来源放行**（README:455）
- 管理面板的地址就是 `http://101.34.65.254:35430/`，**纯 http，没有 https**，等于**密码是明文在网上传的**
- README 里甚至自己把"上 HTTPS"列进了"以后要做"的清单（README:469），说明确实还没做

打个比方：你家门锁是好的，但**门牌号挂在网上，而且门是那种一推就开的**。互联网上有一类程序专门挨个 IP 挨个端口去敲（叫端口扫描），敲到你 35430 就能看见你的登录页。

**不过这事很好解决**，而且**不用给任何人添麻烦**——往下看第七节，我给你排了三个档次的做法。

---

## 一、赶紧修的 7 件（P0）

### 1. 有人不登录也能看到客户的名单【最要紧】

你的服务器上有个查询接口，正常的用法是手机端带着"自己是谁"去查自己的记录。但这个接口有个漏洞：

**只要在网址里填一个假的工号，再带个分组编号，就能把整个分组的客户记录全读出来**——不用账号密码。

拿到的东西是什么？客户姓名、手机号、来访事由。等于**你柜台上的客户本被人抄走了**。

而且这条路**门槛低到离谱**：不用密码、工号随便填、分组编号就是 1、2、3 这样的小数字。

这条直接违背你说的"只有我能访问"，所以我放在第一位。
（位置：`cloud_relay_v2.py:2658` 和 `:2679`）

### 2. 任何人都能往你的数据库里塞假数据

还有两个接口（上报通话记录、上报操作日志）**完全不检查身份**，也不检查"你说你是谁"是不是真的。

在公网上，懂点技术的人可以：
- 批量塞进去一堆假通话记录
- 造出根本不存在的手机设备

**后果**：你面板上的"今天拨了多少通""接通率多少"这些数字会**变成假的**，你自己都不知道该信哪个。
（位置：`:2349` 和 `:2391`）

### 3. 电脑端的短信窗口会卡死，只能关掉重开

坐席用电脑端发短信，发出去之后如果手机那边没回音（比如短信被取消、手机刚好断网、超过时间没响应），**窗口就永远停在"等待手机确认…"，按钮点不动了**。

代码里的问题是：遇到这种情况它**只在后台日志里写一行字，不告诉界面"失败了"**，界面就一直等。

这不是偶发——手机没回执是**经常发生**的事。用户唯一的自救办法是关掉窗口再打开，而且完全不知道为什么。
（位置：`main.js:498`）

### 4. 电脑端的配置会莫名其妙全部清空

电脑端把设置存在一个小文件里。如果这个文件在一次断电/强制关机/崩溃中写坏了，**下一次启动它会悄悄把设置重置成默认值，还把默认值覆盖写回去**。

**丢的是什么**：配对码（得重新配对所有手机）、云服务器地址、手机备注、主题、开机自启——**全没了，而且一句提示都没有**。

用户只会发现"咦，怎么要重新配对了"。
（位置：`modules/settings.js:50` 和 `:76`）

### 5. 电脑端连不上云之后，会"自己放弃"，不会再试了

电脑端连云端是"断了就重试，一次比一次等得久"。但**累计失败大概 1 小时之后，它就彻底不试了**。

所以如果晚上网络抖了一下，或者云端半夜维护，**第二天电脑端是不会自己恢复的**——表现就是"手机连不上电脑，点了拨号没反应"，只能重启程序。
（位置：`modules/cloud.js:83`）

### 6. 手机重连之后，会出现"显示在线但拨不出去"

手机从一个 WiFi 换到另一个 WiFi（或切到流量）会重连。重连的时候，**旧连接断开的那一下，会把刚建好的新连接一起关掉**。

用户看到的现象是：**手机上明明显示"已连接"，点拨号就是没反应**。

顺便还带两个毛病：设备列表里会留下**永远删不掉的幽灵设备**，"云在线"的显示也会乱。
（位置：`phone-connection-manager.js:264` 和 `modules/server.js:335`）

### 7. 安卓手机被系统"杀后台"后，就再也不连回来了

国产手机（小米、华为、OPPO 等）为了省电，会时不时把后台程序清掉。

你们的安卓端**被清掉之后没有任何"自动复活"的机制**——没有用系统提供的定时任务、闹钟这些能力，只有一个"开机自启"。

所以现象是：**客服以为手机还在线，其实早就断了**，别人打电话进来没反应，而且**没人会发现**。
（位置：整个工程里搜不到 `WorkManager`/`AlarmManager`）

---

## 二、有空该修的 15 件（P1）

这些不是天天碰到，但**一碰到就麻烦**。

### 安卓手机端（4 件）

| 问题 | 什么时候会遇到 |
|---|---|
| 点了"手动断开"，**重开 App 又自己连上了** | 用户手点断开后重开程序。用户会觉得"这开关没用啊" |
| 通话记录**权限被拒之后，App 里没有"去授权"的按钮** | 用户不小心点了"拒绝"。之后记录**悄悄不上传**，面板上今天的数据是 0，你也不知道为什么 |
| 通话记录页的"重拨"**不走正规通道** | 客服在通话记录里点重拨。这个号码不走选卡规则、不留记录、不给电脑回执，**也不进统计** |
| 云端返回空数据时，**会把手机上已存的上门记录清空** | 云端数据被清过、或接口出过一次错，管理员一点"同步"——**本地的记录就没了** |

### 浏览器插件（3 件）

| 问题 | 什么时候会遇到 |
|---|---|
| 拨号按钮可能**拨给上一个客户** | 客服系统是"不刷新页面、只换内容"的那种设计时，按钮绑的是旧号码。后果是**拨错人，骚扰到客户** |
| 拨号浮窗**连点两次就拨两通** | 客服手快，或者习惯双击 |
| "测试连接"点了之后**永远转圈** | 云服务器不是拒绝你、而是"没回应"的时候，按钮会一直卡在"测试中…" |

### 电脑端（2 件）

| 问题 | 什么时候会遇到 |
|---|---|
| 选了"关闭就退出"，结果**程序没退、悬浮条还在桌面上**，但托盘图标没了 | 用户主动改过这个设置 |
| 每次拨号**会覆盖你剪贴板里的东西** | 坐席先复制了别的资料再去拨号，复制的东西就没了 |

### 安全相关（4 件，跟"只有我能访问"有关）

| 问题 | 说明 |
|---|---|
| 手机端和云端之间是**明文传输** | 客户数据和工号在网线上"裸奔"，在公共 WiFi 下可能被人截到 |
| 登录/登出还是**允许用网址传密码** | 面板那边已经改成正规方式了，但服务器这头还留着老路，密码可能进浏览器的历史记录和服务器日志 |
| 手机上报通话记录**还是用网址传一大堆数据** | 数据会进服务器访问日志，而且网址太长可能被中间环节截断 |
| **压测脚本写死了生产服务器地址，而且不清测试数据** | 哪天有人手滑跑一下，就把假工号和假记录**塞进你的正式数据库**了 |

### 管理面板（2 件）

| 问题 | 什么时候会遇到 |
|---|---|
| 首页"最近登录的设备"那一栏，**有两个字段没做转义处理** | 需要有人能连上你的公网端口并伪造数据。配合上面的安全组限 IP 一起解决 |
| 通话记录页的**"设备筛选"下拉框不刷新** | 新设备注册后，不重启面板就选不到它 |

---

## 三、顺手改的 20 件（P2）

**用户完全感觉不到**，但改动很小，做别的改动时一起带上就行。比如：

- 有些缓存不过期、有些弹窗按 Esc 关不掉、有些字太小太淡
- 手机端断网期间的记录会延迟上传（重连后会自动补齐，不会真丢）
- 面板上有些信息重复展示了三遍、主题有 10 套可选（太多了）
- 日志里会明文写工号（得日志文件被别人拿到才有风险）

---

## 四、不用管的 25 件

### 最大的一块：那套 Go 语言的电脑端，整个可以扔了

`pc-app-go/` 这个目录里的东西：

- **根本编译不出来**——它要打包一个不存在的前端目录，任何机器上跑 `go build` 都会直接报错
- 你们自己的变更记录里**明确写过"Go 版不动"**
- 三份历史报告里都写着"**当前电脑端主力是 Electron 版**"
- 仓库里那个 exe 是 8 月的老产物，**跟现在的源码已经对不上**

所以它的 **9 个问题一个都不用修**。建议直接在 README 里标一句"实验版、已停止维护"，或者整个挪进历史归档目录。

### 核查后发现"其实不是问题"的 2 件

- 说"拨号结果广播不到 iframe 里的浮窗"——**实际不成立**，浮窗本来就只在最外层创建，广播正好能到
- 说"Docker 重建日志就丢"——按你们**现在用的官方部署方式其实不会丢**（Dockerfile 里已经指到挂载卷了），只有不用官方方式裸跑才会

### 场景下不值得花时间的 14 件

比如"换 SIM 卡后会拨错卡"（你们 20 个人基本不换卡）、"自动选卡只认小米"（得先看你这边机型是不是统一的）、"面板字号 11px 偏小"这类。

**其中有一个例外，我单独说一下**：电脑端有一处"防火墙提示"是死代码——它的作用是，如果程序没拿到管理员权限、没能自动放行局域网端口，本来应该**弹一句"局域网可能连不上"提示你**。现在这个提示不会弹。这个建议顺手补上，因为你们刚好开着局域网直连功能。

还有一个要你确认的：**电脑端"开机自启"在便携版上会失效**（因为它记的是临时解压目录的路径）。这取决于你们实际发的是哪种包——如果是文件夹版就没事，如果是单文件便携版就有问题。

---

## 五、我建议的动作顺序

**第一批（这周，6 件）**——都是"疼"的：

1. 堵上那个不用登录就能看客户名单的漏洞
2. 修电脑端短信窗口卡死
3. 修电脑端配置被清空
4. 修安卓被杀后台后不自恢复
5. 修手机重连后"显示在线却拨不出去"
6. 修电脑端连不上云之后自己放弃

**第二批**——数据别丢、别拨错人：
上报接口加身份校验、返空别覆盖本地记录、修拨给上一个客户、重拨走正规通道、手动断开能生效、权限被拒给个入口、"关闭即退出"真的退

**第三批**——安全收口：
把密码从网址里拿掉、上报改走正规方式、给公网传输加个锁（HTTPS）

---

## 六、关于"云端只有我能访问"，我推荐你先做这件事

按花的力气从小到大，三个办法：

| 办法 | 要多少力气 | 效果 |
|---|---|---|
| **【最推荐】去腾讯云控制台，把 35430 端口的"允许来源"从"所有人"改成"只允许公司网络的 IP 和你的常用 IP"** | **改几条规则，5 分钟** | 外面的人**根本扫不到**你的端口，上面说的"不用登录就能看名单""能灌假数据"这两条**直接从源头解决**。关键是——**完全不增加任何人的操作步骤** |
| 改代码：堵越权漏洞 + 不再用网址传密码 | 半天 | 就算被扫到，也读不到也灌不进 |
| 加 HTTPS（用免费证书） | 1~2 天，还得管证书续期 | 密码和客户数据不再明文过网，但要处理手机端的证书信任 |

**我的建议**：先做第一个。你本来就是小范围内部用、服务器又握在自己手里，**限 IP 是投入产出比最高的一步**，而且正合你说的"不能太增加体验的复杂程度"。代码那边就只堵那一个越权漏洞就行。

唯一的前提是：**客服会不会在外面（比如咖啡馆、家里）用自己的手机流量拨号？**
- 如果**只**在公司和固定网络里用 → 直接限 IP，一步到位
- 如果**有**人要在外面用流量 → 那限 IP 会把他们挡在外面，只能走"改代码"那一档

这一点你确认一下，我就能给你出具体的方案。

---

## 七、需要你拍板的三件事

1. **客服会不会在外面用手机流量拨号？**（决定能不能用"安全组限 IP"这个零成本方案）
2. **电脑端你们实际发的是哪种包？**单文件便携版还是文件夹版？（决定那个"开机自启失效"要不要修）
3. **公司里在用的手机品牌统一吗？**（决定"自动选卡只认小米"要不要改）

这三个问题回答完，我就能把第一批 6 件的具体改法给你。

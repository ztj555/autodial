# 评审意见：修复方案-配对码遗留问题（基于实际代码核对）

> 评审对象：`修复方案-配对码遗留问题-2026-09-10.md`
> 评审方式：逐条对照本工作区实际代码（ConnectionManager.kt / DialService.kt / cloud_relay_v2.py / 扩展 background.js）
> 使用约束：公司内部约 20 人使用，**便捷性优先于安全加固**，云端管理只有管理员一人
> 结论：方案两个根因判断**全部属实**，可以执行；但 B1 的 deviceId 取值建议改用 App 现成的 UUID，迁移工作量近乎归零。

---

## 一、代码核对结果（方案与实际是否相符）

| 方案条目 | 代码证据 | 核对结果 |
|---|---|---|
| B：云端以 deviceName 作设备唯一键 | ConnectionManager.kt 778、919 行 hello 只发 `deviceName = Build.MODEL`；cloud_relay_v2.py 675 行存入 meta、693 行 `_get_device_default_pin(device_name)` 查 phones 表 | ✅ 属实 |
| A：手机端不处理 auth_pending | 云端 755-762 行发送 auth_pending；ConnectionManager.kt Cloud 分支 when（929-982 行）无此 case → 落入 `else -> notifyMessage`；DialService.kt onMessageReceived（245-285 行）只处理 dial / reconnect_request / sms / hangup → **静默丢弃**，干等 120 秒后收 auth_fail | ✅ 属实 |
| B3 所需后台 API 已具备 | `/api/v1/devices`（2414 行）、`/api/v1/device-set-default-pin`（2496 行）已存在且都有 admin 鉴权 | ✅ 无需新增接口 |
| 授权超时 120 秒 | 391 行清理 >120s 的 _pending_auths 并发 auth_fail；1033 行 finally 清 stale | ✅ |
| 方案称"ConnectionManager 已有 deviceId 概念可复用" | ConnectionManager.kt 本身**没有** deviceId；但 **PrefCtrl.getDeviceId()（device_uuid，PrefCtrl.kt 44-50 行）已存在**，且 DialService 的通话上报（836 行）、事件上报（912 行）、统计上报（889 行）全都在用它 | ⚠️ 说法不准，但恰恰是**改进机会**（见下） |

---

## 二、关键修订建议：deviceId 改用 App 现成的 UUID，弃用"型号-ANDROID_ID后6位"

**发现**：手机端每个上报请求（calls/batch 2057 行、stats/report 2132 行、events/log）都在用 `device_uuid` 往云端 `phones` 表 UPSERT 记录，且 stats/report 会写入 `device_model`、`app_version`、`last_pin`。也就是说：**phones 表里现在就已经有每台活跃手机的 UUID 行（带型号、最近用过的 PIN）**，管理后台设备列表已经能看到它们。

**因此建议 B1 改为**：

```
两处 hello（LAN 776 行、Cloud 917 行）增加：
put("deviceId", PrefCtrl(context).getDeviceId())   // 复用现有 device_uuid
put("deviceName", Build.MODEL ...)                 // 保持展示名不变
```

收益（对 20 人 + 单管理员场景）：
1. **零新增身份代码**——不再写 resolveDeviceId() 的 ANDROID_ID 逻辑。
2. **身份统一**——握手、通话、事件、统计全部归到同一条设备记录，不再是"型号名一行 + UUID 一行"两套账。
3. **迁移 B3 变成一次后台点选**——后台设备列表里 UUID 行已带型号和 last_pin，逐台设好 default_pin 即可，20 台半小时内完成；方案中的"选项 b SQL 改名"彻底不需要。
4. 兼容性结论不变：旧 APK 连新云端回退 deviceName（旧行为）；新 APK 连旧云端多余字段被忽略。

代价（可接受）：卸载重装后 UUID 变化 → 该设备显示"未在云端注册"，管理员在后台重新预设一次。内部 20 人、管理就是你自己，这不是负担。若想彻底免维护可再叠加 ANDROID_ID 兜底，但不建议为此增加复杂度。

**B2 落地时方案漏掉的两处同步修改**：
- `/api/v1/devices` 的 online_map（2425-2443 行）目前按 `device_name` 建 key 判断在线状态，须改为按 `device_id`（meta 中需同时存 `meta['device_id']`），否则升级后设备列表"在线"列全错。
- 884-890 行 RESEND phone_hello 给新连 PC 时 `'deviceId': phone_device_name` 也要换成 device_id（与 831 行转发逻辑一致）。

---

## 三、结合"便捷优先、云端只有你管理"的取舍判断

1. **设备-PIN 绑定（default_pin）建议保留，不简化掉**。它的真实价值不是防外部攻击，而是**防同事手滑输错 PIN**：输错会顶掉别人会话（682-688 行踢旧连接）、电话打到别人手机上。自己用绑定 PIN 连接时走 768 行直通 auth_ok，**没有任何额外操作**——零便捷性损失。

2. **不建议加"未知设备自动注册"开关**。那会把"输错 PIN 的第一台手机"直接绑到错误 PIN 上，制造新的混乱源；而后台预设 20 台只是一次性动作。

3. **A（auth_pending 等待提示）按原方案执行即可**，与代码吻合；不新增 ConnectionState 枚举、LAN 侧不动的范围界定也正确。

4. **针对单管理员，建议补一个可选增强 C4**：后台加"待授权列表 + 管理员代批"。目前 `/api/v1/auth/respond`（1845 行）要求响应者 PIN == 目标 PIN，即**只有对方 CRM 页面开着才能授权**——这是整个流程里唯一的死路。给 admin token 加一个旁路，你随时能在后台替员工放行，比喊对方开浏览器更符合"管理只有你一个人"的现实。

5. **C1（PC 弹窗授权）优先级可降**：扩展 background.js 85 行已经在 5 秒轮询 `/api/v1/auth/pending` 并弹窗，授权入口已覆盖，PC 端 cloud.js 里没有 auth_request 处理（已核实），不加也不影响可用性。

6. **顺手核查项**：`admin_accounts` 表存在默认账号（289 行 `_seed_default_admin`），确认只有你自己的账号、默认密码已改（`/api/v1/admin/accounts` 可查）。

---

## 四、修订后实施顺序

1. B1'（hello 加现有 UUID）→ B2（含 online_map、RESEND 两处补改）→ B3'（后台给 20 台 UUID 行预设 default_pin，一次性）
2. A1 → A2 → A3（授权等待 UI，照原方案）
3. 可选：C4 管理员代批 > C2 限流提示 > C1 PC 弹窗 > C3 倒计时
4. 按原方案第 7 节清单回归；先云端后 APK 灰度顺序不变

回归清单补充一条：
8. 升级后管理后台设备列表：在线状态、型号、最近 PIN 显示正确（对应 online_map 改 key）。

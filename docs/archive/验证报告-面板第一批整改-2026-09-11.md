# 验证报告 — 管理面板第一批整改

- **日期**：2026-09-11
- **范围**：AutoDial 云端管理面板（`dashboard.html`）+ 云中继后端（`cloud_relay_v2.py`）第一批整改
- **线上环境**：腾讯云 `101.34.65.254`，35430 主实例（Supervisor 托管）
- **验证方式**：部署取证 + 隔离环境实测（不碰生产数据）+ 代码级人机工程复核

---

## 一、部署结果：✅ 成功（未回滚）

| 项目 | 结果 |
|---|---|
| `cloud_relay_v2.py` MD5 | 本地 `c84bd61fb59f9a2238f3b27635fb6409` == 云端 同值 ✅ |
| `dashboard.html` MD5 | 本地 `8c57c583fdeeda52bf78aba1e7730a3a` == 云端 同值 ✅ |
| 远程语法预检 | `python3 -m py_compile` 通过后才重启（失败即还原的闸门生效） |
| 服务状态 | `autodial RUNNING`（pid 3324838，16:01 启动） |
| 健康检查 / 首页 | `/health` 正常；首页 HTTP **200** |
| 重启后日志 | 无新增 Traceback / ImportError / TypeError / SyntaxError |
| 旧版备份 | `/opt/autodial/{cloud_relay_v2.py,dashboard.html}.bak-20260911-batch1` |

**回滚命令**（一条即可）：
```bash
sudo cp /opt/autodial/cloud_relay_v2.py.bak-20260911-batch1 /opt/autodial/cloud_relay_v2.py && \
sudo cp /opt/autodial/dashboard.html.bak-20260911-batch1 /opt/autodial/dashboard.html && \
sudo supervisorctl restart autodial
```

---

## 二、后端验证：✅ 全部通过（隔离环境实测）

**方法**：用 `VACUUM INTO` 对生产库做**只读快照** → 拷到 `/tmp` → 清空快照里的管理员 → 以环境变量创建已知口令的临时账号 → 用 `AUTODIAL_DB_PATH=/tmp/...` + 独立端口（35498/35499）启动临时实例实测 → 跑完彻底清理。**全程物理上无法写入生产库。**

| # | 验证项 | 结果 |
|---|---|---|
| 1 | 无 token 访问 `/api/v1/calls/export` | **401** `UNAUTHORIZED` ✅ |
| 2 | 正常导出 | `200 OK` + `Content-Type: text/csv; charset=utf-8` + `Content-Disposition: attachment; filename=calls_20260911_163428.csv` ✅ |
| 3 | BOM | 首字节 `ef bb bf` ✅（Excel 打开中文不乱码） |
| 4 | 中文表头 | `设备ID,号码,通话时间,时长(秒),类型,SIM卡,机型,版本` ✅ |
| 5 | 时间格式 | 毫秒时间戳 → `2026-06-01 08:00:00`（本地时区，正确）✅ |
| 6 | 筛选 `device_id` | `QA_DEV_A` → 2 条；`QA_DEV_B` → 2 条 ✅ |
| 7 | 筛选 `date_from~date_to` | 2026-01-01 → 3 条；2026-06 月 → 1 条 ✅ |
| 8 | CSV 公式注入防护 | `=1+1` → `'=1+1`；`@SUM(A1)` → `'@SUM(A1)` ✅ |
| 9 | 返回头 | `Access-Control-Allow-Origin: *` ✅ |
| 10 | B1 修复取证 | `grep group.pcs` → 第 868 行 `list(group.pcs \| group.phones)`；旧写法出现 0 次 ✅ |

**生产数据体检（只读）**：`visits 2560`、`phones 101`、`advisor_names 15`、`admin_accounts 1`、`call_records_raw 0`、`phone_events 0`，管理员 `18335162275` —— 与整改前一致，**QA 过程未损坏任何生产数据**。

### 顺带发现（非缺陷，但影响判断）
- **`call_records_raw` 表当前是 0 行**（手机端尚未上报过通话记录）。因此"通话记录"页与导出功能目前都是空表——功能正确，但**业务上尚无数据**。若你预期这里应该有数据，那说明手机端上报链路可能没打通，值得单独排查。
- `/health` 返回的 `"version": "4.10"` 是代码里写死的旧字符串，**不代表真实版本**（判断版本要看 MD5/CHANGELOG）。
- 临时实例日志出现 `添加防火墙规则错误: ... 'netsh'` —— Linux 上调 Windows 专用命令的**历史无害噪音**，建议加平台判断（已知项）。

---

## 三、遗留清理与环境排查：✅ 已处理

| 项 | 情况 | 处理 |
|---|---|---|
| QA 遗留临时实例 | 端口 35499 上挂着 `/usr/bin/python3 /tmp/qa_relay.py --port 35499`（16:07 启动，已变孤儿进程 PPID=1），**第一轮清理未杀掉**（脚本被改名为 `qa_relay.py`，`pkill` 模式不匹配） | 已 `pkill -f qa_relay.py` 杀掉，35499 已释放 |
| `/tmp` 残留 | `qa_relay.py`(148KB)、`qa_exp_full.csv`、`qa_hdr_full.txt`、`qa_relay_stdout.log` | 已全部删除 |
| root pid 3083791 | 一度怀疑是异常进程 | 查明：`cwd=/app`、父进程为 `containerd-shim`(namespace `moby`) → **就是 35440 的 Docker 容灾实例**，正常，未触碰 |
| 最终状态 | — | 仅 35430 主实例（RUNNING，首页 200）+ 35440 容器，无其它残留 ✅ |

**关于今天电脑上弹出的终端窗口**：为 `WindowsTerminal`（pid 8772，16:09:31，父进程 `svchost`、带 `-Embedding`），是后台自动化任务触发控制台时被 Windows 默认终端机制包装出的窗口，**无子进程、未干扰任何程序**，已关闭。

---

## 四、前端人机工程复核（代码级）

> ⚠️ 说明：本节为**代码级复核**结论。真实浏览器渲染实测（截图/交互）尚未执行——自动化浏览器任务在当时弹出了终端窗口，为不干扰你使用已主动中止。

### 4.1 本批 8 项改动的实际效果评估

| 项 | 评价 |
|---|---|
| F1 统一请求层 + 顶部横幅 | 设计合理：401/429/非2xx/断网四类分开提示，成功自动收起（有 `apiBannerShown` 短路，不会每 15s 闪一下）。**唯一遗憾：429 只提示不退避**，15 秒后照常再发，可能持续撞限流 |
| F2 补齐静默 catch | 到位：失败报错 + 分组/姓名失败自动重拉（保证"界面显示 = 真实值"）；成功也给明确反馈 |
| F3 移除假保存 | 到位：设置页改为只读 + 显示真实端口（走 `/api/status`），并写清"需服务器端改+重启"。**诚实优于好用的正确取舍** |
| F4 导出改服务端 | 到位：与上门登记页行为一致；已验证 200/BOM/表头/筛选/注入防护全通过 |
| F5 表格横向滚动 | 思路正确（按列数×92px 设最小宽度）。**待实测**：11 列的上门登记表在 1280px 下可能仍触发滚动条，属预期行为但需确认观感 |
| F6 搜索防抖 | **只改了一半**——手机搜索已防抖，但**系统日志的"关键词搜索"仍是 `oninput="loadLogs()"` 无防抖**（见 4.2 P0-3） |
| F7 补 401 处理 | 到位：设备/通话/日志/首页走统一层，登记页与人员页单独补了 401 分支 |
| F8 新增 CSS | 正常，未与既有 `.data-table` 规则冲突 |

### 4.2 仍然存在的问题（建议作为第二批）

**P0 — 影响正确性 / 安全 / 明显卡顿**
1. **管理员密码明文显示**：`new-admin-pass` 与 `chpwd-newpass` 都是 `type="text"`（HTML 约 709/722 行）。旁边有人直接看得见 → 应改 `type="password"` 并提供"显示"切换。
2. **自动刷新会吞掉正在输入的内容**：`refreshAll()` 每 15 秒调 `showPage(id)` → 重建整张表，会清掉设备页正在编辑的"别名/默认PIN"输入框，也会丢失滚动位置；**且没有"暂停自动刷新"开关**。运营人员边看边改时体验很糟。
3. **日志搜索无防抖**：`#log-search` 的 `oninput="loadLogs()"` 每按一键就请求一次（可拉 100~1000 行日志并解析）。这是本轮唯一漏掉的同类问题，建议与手机搜索统一为 400ms 防抖。

**P1 — 效率与反馈**
4. **限流根因未解**：面板自身一轮 `loadDashboard` 就打 4 个接口（15 秒一轮 ≈ 16 请求/分钟），若与员工客户端共用同一出口 IP，仍会撞 60/分钟/IP 的限流。本批只是"提示得更清楚" → 建议把严格限流只留给认证端点，业务端点改按 PIN/设备维度计。
5. **人员管理表多出一个空白"操作"列**（表头有 `<th>操作</th>`，行内 `<td></td>` 为空）→ 纯视觉噪音，还暗示"这里该有按钮"。
6. **手机页"详情"按钮名不副实**：`showPhoneDetail()` 只弹一个 toast，不展示任何详情内容（真正的历史记录靠点行展开）→ 建议删除该按钮或改为打开详情弹窗。
7. **改密码要手输数字账号 ID**（`chpwd-id`）→ 应改为账号下拉选择，避免记 ID / 输错。
8. **危险操作全用原生 `confirm()`**（删记录/账号/分组）→ 样式割裂、无法展示对象名称。建议统一自定义确认弹窗，并在弹窗里回显"将要删除：XX"。
9. **无 URL 路由**：`showPage()` 不写 hash，F5 后一律回首页 → 值守时刷新要重走导航。

**P2 — 可读性与细致度**
10. **字号与对比度**：表头 `11px`、次要文字 `11px`，次要色对比度约 **4.1:1**（低于 WCAG AA 的 4.5:1）→ 长时间值守易疲劳、看错。建议表头 12px、次要色提到 4.5:1 以上。
11. **手机页无分页**：`loadPhones()` 一次性渲染全部设备（当前 101 台，`colspan=10` 的 10 列表），叠加 15 秒重建 → 设备增长后会明显卡顿。
12. **弹窗细节**：`visit-edit-modal` / `import-modal` 缺少 Esc 关闭与焦点陷阱，键盘操作不友好。

---

## 五、结论与下一步

**结论**：本轮部署成功、后端新功能与修复项**实测全部通过**、生产数据完好；环境残留已清理干净。前端人机工程的 8 项改动方向正确，但复查出 **12 条仍待处理项**（3×P0 / 6×P1 / 3×P2）。

**建议下一步**（按性价比排序）：
1. 一次性修掉 3 条 P0（密码框类型、自动刷新暂停开关、日志搜索防抖）——改动都很小，收益最直接。
2. 修 P1 的"限流按 PIN 维度"——这是"员工拨不出去/登记失败"的根因，比面板观感更重要。
3. 需要时再做一次**真实浏览器渲染实测**（建议用无头模式，避免弹窗干扰），把 4.1 中标注"待实测"的项补齐。

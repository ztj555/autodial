# 云端管理面板 · 真实数据实测报告（凭据登录后）

**日期**：2026-09-11
**环境**：腾讯云 101.34.65.254 / Ubuntu 22.04 / 35430 主实例（Supervisor 托管）
**线上版本**：`dashboard.html` = `b782cfa1…`、`cloud_relay_v2.py` = `c84bd61f…`（本地 == 云端）
**方法**：用管理员凭据完成 E2E 登录 + 以 token 逐个调用面板各页依赖的接口（只读）

---

## 一、凭据确认

| 项 | 结果 |
|---|---|
| 账号 | `18335162275`（与手机号同号）✅ |
| 口令 | `123456` ✅ |
| 验证方式 | ① `GET /api/v1/login` 返回 `{"ok":true,"token":…}`；② 哈希反推：库中 `e488babc1673…` 恰等于 `sha256('autodial#v4' + '123456')`，**双证据一致** |
| 浏览器 E2E | 面板登录表单填入后点击「登录」→ 注入 token → 首页真实数据渲染成功（端口 35430 / 运行 0时47分 / 今日上门 35） |
| 控制台 | 加载期 **0 个 JS 报错、0 条 console 输出** |

---

## 二、逐页数据供给核对（11 个接口全部 HTTP 200）

| 面板页 | 依赖接口 | 状态 | 实际数据 |
|---|---|---|---|
| 首页 | `/api/status` | 200 | service/port/uptime 正常 |
| 首页 | `/api/history` | 200 | 9.5KB，30 秒粒度采样 |
| 首页 | `/api/stats` | 200 | 累计消息 11888 |
| 首页 | `/api/clients` | 200 | `[]`（当前无客户端在线） |
| 手机管理 | `/api/v1/devices` | 200 | 30.7KB，101 条（见下方发现①） |
| 通话记录 | `/api/v1/calls` | 200 | **`total: 0`（空表）** |
| 上门登记 | `/api/v1/visits` | 200 | 165KB，最新 id=2599（17:46 仍在写入） |
| 人员管理 | `/api/v1/pins` | 200 | 20 位坐席姓名 |
| 分组 | `/api/v1/groups` | 200 | `[]`（未使用分组功能） |
| 系统日志 | `/api/logs` | 200 | 8.7KB，含限流告警 |
| 管理员 | `/api/v1/admin/accounts` | 200 | 1 个账号 |

> 结论：**面板的数据通道全部健康**，没有出现"接口坏掉但界面显示暂无数据"的情况。空表（通话记录/分组/客户端）是真实无数据，不是故障。

---

## 三、三个关键发现

### 发现① 生产库被压测数据污染：101 台设备里 100 台是假的 🔴

```
phones 总数        : 101
疑似压测设备       : 100   ← device_id = ZZLOADTEST-P003…P100，last_pin = 19900000003…
唯一真实设备       : 23127PN0CC（pin 18335162275，2026-09-10 接入）
伪造数据写入时间   : 2026-09-11 14:10:06（一次性批量写入）
```

- 来源与 14:11 那批 `REST_RATE_LIMITED`（227 次）**时间完全吻合**，确认是 `test_stress_live.py` 打到了**生产库**，而非独立测试环境。
- 后果：手机管理页展示的是压测假数据，管理员无法据此判断真实设备情况；此前我向你汇报的"101 台设备"里 **只有 1 台是真的**，特此更正。
- 建议：压测脚本必须指向独立 DB（`AUTODIAL_DB_PATH=/tmp/…`）或加 `--dry-run`；生产库中 100 条 `ZZLOADTEST-*` 建议清理。**清理属删数据操作，需你确认后我再执行**，清理前会先备份 `visits.db`。

### 发现② 通话记录链路确实从未产生数据（非面板问题）

```
call_records_raw = 0 行
phone_events     = 0 行
当前在线客户端   = 0（/api/clients 为空）
```

手机端注册过 1 台、云端 35430 正常，但**上报链路 0 产物**。问题在手机端/PC 端的上报逻辑或触发条件，不在云端与面板。面板的"通话记录"页与导出功能本身实测正确（第一批已用隔离实例验过）。

### 发现③ 暴露面与凭据风险（安全，建议尽快处理）🔴

| 项 | 现状 | 风险 |
|---|---|---|
| 管理面板 | `http://101.34.65.254:35430/` 纯 HTTP，无 TLS，ufw `35430/tcp ALLOW Anywhere` | 口令与 token 全程明文过公网 |
| 登录方式 | `GET /api/v1/login?user=&pass=` **口令写在 URL 里** | 口令进入浏览器历史、中间设备日志 |
| 口令强度 | `123456`（全球最常见弱口令 Top1） | 结合下方 9001 暴露，风险叠加 |
| **Supervisor Web UI** | `0.0.0.0:9001` 对外，Basic 认证 `ubuntu` / **与服务器 SSH 同一口令** | 9001 凭据泄露 = **直接拿到 SSH 服务器权限** |
| 口令存储 | `sha256('autodial#v4' + pwd)`，盐值**硬编码在源码** | 单轮 SHA-256 + 固定盐，字典攻击成本极低 |
| 1Panel | `0.0.0.0:8090` 对外 | 需确认是否有强口令 + 是否必须公网可达 |

**好消息**：日志中 `ADMIN_LOGIN` 成功 11 次、**失败 0 次**，历史无爆破痕迹；17:35/17:37/17:44/17:49 四条是我本次实测登录。

**建议（按性价比排序）**：
1. 改掉管理员口令，**不要用 123456**；同时改掉 supervisor 9001 的口令，且**不要与 SSH 口令相同**。
2. 9001 / 8090 **从公网收回**：ufw 仅放行你的常用出口 IP，或走 SSH 隧道访问。
3. 登录改 `POST`，口令放请求体，不再进 URL。
4. 中长期：给 35430 加 TLS（或前置反代 + 证书），避免明文。

---

## 四、本次未能完成的验证（如实说明）

- **逐页截图不完整**：`agent-browser` 守护进程在本机多次超时/崩溃（已 `doctor --fix` 修过一次，长脚本求值仍会触发），画面截图落盘为空白。因此**没有拿到各页的可视化截图**。
  已用等效的确定性证据替代：① 首页 DOM 真实值（`hdr-port=35430`、`uptime=0时47分41秒`、`today-visits=35`）；② 逐页依赖接口全部 200 + 真实数据；③ 加载期 0 JS 报错。
- **P0 修复的浏览器实证**沿用上一轮已完成的结果（密码遮罩、暂停开关、防抖 8→1、表格横向滚动 800/1280px），本轮不重复。

---

## 五、给业务方的两条实况提醒

- 上门登记**正在被真实使用**：`VISIT_CREATE pin=18335162275 name=邱华泉 id=2599` 发生在 17:46，且日志出现 `VISIT queued (offline)`——坐席当时**处于离线态**，数据先落队列。若坐席频繁离线，需关注手机端保活。
- 16:54 那批限流拦截**打在了 `/api/v1/visit`（上门登记）上**，与"补录上门账号"的实时录入重叠。限流按 IP 计（同办公室共用出口）这条仍是影响真实业务的首要隐患。

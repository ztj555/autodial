# 管理面板 P0 修复 · 部署与实测验证报告

- 日期：2026-09-11
- 目标：腾讯云 `101.34.65.254` · 端口 `35430` · Supervisor 托管（program `autodial`）
- 本批内容：3 条 P0 人机工程问题修复 + 部署上线 + **真实浏览器实测**
- 版本：`dashboard.html` MD5 `b782cfa1f2e45a950a0ea1f2751ad53b`；`cloud_relay_v2.py` 未改动 `c84bd61fb59f9a2238f3b27635fb6409`
- 结论：**部署成功，3 条 P0 全部实测通过，无回归，无新增异常**

---

## 一、本批修了什么

| 编号 | 问题 | 修法 | 位置 |
|---|---|---|---|
| P0-1 | 管理员密码框是 `type="text"`，**旁边的人能直接看见明文** | 两处改 `type="password"` + `autocomplete="new-password"`，占位文案同步为"至少4位" | `#new-admin-pass`、`#chpwd-newpass` |
| P0-2a | 15 秒自动刷新会**重建整张表**，正在填的设备别名 / 默认 PIN 被冲掉 | 新增 `isBusyEditing()`：焦点在**表格内**或**弹窗内**的输入框时，定时刷新自动跳过，并在"上次刷新"处标注「已跳过（正在填写）」。筛选框（搜索类）**不算**，不会误阻 | `refreshAll()` / `isBusyEditing()` |
| P0-2b | 自动刷新**没有暂停开关**，长时间录入无法摆脱打断 | 顶部"自动刷新 · 每15秒"徽标改为**可点击按钮**，点击暂停/恢复；暂停态变橙色并显示"自动刷新已暂停 · 点击恢复" | `#refresh-label` / `toggleAutoRefresh()` |
| P0-3 | 系统日志关键词搜索**每按一键就请求一次**（可拉 1000 行日志） | 加 400ms 防抖，与手机/通话搜索口径一致 | `#log-search` / `logSearchDebounce()` |

本地校验：Python `compile()` 通过；两个内联 `<script>` 提取后 `node --check` 全部通过。

---

## 二、部署取证（生产）

| 项 | 结果 |
|---|---|
| 云端备份 | `/opt/autodial/cloud_relay_v2.py.bak-20260911-batch2`、`dashboard.html.bak-20260911-batch2` |
| 上传后 MD5 | py `c84bd61f…`、html `b782cfa1…`，**本地 == 云端** ✅ |
| 语法预检 | `python3 -m py_compile` → `PY_COMPILE_OK`（失败即还原、不重启的闸门生效） |
| 重启 | 16:50:07，`autodial RUNNING pid 3337252` ✅ |
| 健康检查 | `/health` 正常；首页 HTTP **200** ✅ |
| 监听 | `LISTEN 0.0.0.0:35430 users:(("python3",pid=3337252))` ✅ |
| 重启后日志 | 以 `Server started on port 35430` 为界，其后 `Traceback/TypeError/ImportError/NameError` = **0**，`[ERROR]` = **0** ✅ |

---

## 三、真实浏览器实测（headless，无弹窗）

工具：`agent-browser`（默认 headless，不弹窗口）。线上真实页面 `http://101.34.65.254:35430/`。

### 3.1 加载期无 JS 报错

用 `--init-script` 在页面脚本之前挂 `error` / `unhandledrejection` / `console.error` 钩子：

```
{"hook":"object","count":0,"list":[]}
```

→ **加载期 0 个 JS 异常、0 个 console 报错**（面板白屏风险排除）。

### 3.2 逐项验收

| 验收点 | 实测结果 | 判定 |
|---|---|---|
| P0-1 `#new-admin-pass` | `type` = `password` | ✅ |
| P0-1 `#chpwd-newpass` | `type` = `password` | ✅ |
| P0-1 `#login-pass`（原有） | `type` = `password` | ✅ |
| P0-2b `#refresh-label` | 标签名 `BUTTON`，带 `title` 说明 | ✅ |
| P0-2b 暂停 | 点击后文案 → `自动刷新已暂停 · 点击恢复`，class → `auto-badge paused` | ✅ |
| P0-2b 恢复 | 再点 → `自动刷新 · 每15秒`，class → `auto-badge` | ✅ |
| P0-3 `#log-search` | `oninput` = `logSearchDebounce()` | ✅ |
| 防抖行为 | **连发 8 次 input 事件 → `loadLogs` 只被调用 1 次**（改前为 8 次） | ✅ |
| 表格溢出容器（第一批） | `table.data-table` 7 张 ↔ `.table-wrap` 7 个，`overflow-x: auto` | ✅ |
| 401 横幅（第一批） | 未登录访问时横幅显示「登录已过期，请重新登录后再查看数据…」+ 重新登录按钮 | ✅ |

### 3.3 自动刷新三种路径（同步实测，直接调用定时器所用的同一条路径）

服务器端接线核对（线上文件）：
```
2269:  refreshTimer = setInterval(function() { refreshAll(true); }, REFRESH_INTERVAL);
2258:  if (fromTimer && refreshPaused) return;
2259:  if (fromTimer && isBusyEditing()) {
```

浏览器内行为实测（用计数器观测 `loadDashboard` 是否被真正调用）：

```
A 无输入聚焦      → 定时刷新真的执行   : true
B 表格内输入聚焦  → 定时刷新被跳过     : false  （"上次刷新"标注：已跳过（正在填写））
C 暂停开关打开    → 定时刷新不执行     : false  （徽标：自动刷新已暂停 · 点击恢复）
```

判定边界（防止"随便点个输入框就停止刷新"）：

```
A 表格内输入        → true   ✅ 应跳过
B 弹窗内输入        → true   ✅ 应跳过
C 普通容器内输入    → false  ✅ 不应跳过
D 无输入聚焦        → false  ✅ 不应跳过
```

### 3.4 视觉取证（截图）

| 截图 | 说明 |
|---|---|
| `admin_pwd_paused.png` | 管理账号页：密码框显示为圆点（**明文风险已消除**）；右上徽标变**橙色**"自动刷新已暂停 · 点击恢复" |
| `visits_800.png` | 上门登记页 800px 宽：11 列表格出现**横向滚动条**（不再压缩变形）；顶部红色 401 横幅正常 |
| `visits_1280.png` | 上门登记页 1280px 宽：布局完整，侧栏 + 内容区正常，表格仍保留横向滚动 |
| `live_locked.png` | 未登录门禁态（首帧截图为空，重开后正常，见"限制"第 3 条） |

表格溢出量化：

| 视口 | 容器宽 | 表格宽 | 是否出现横向滚动 |
|---|---|---|---|
| 800×900 | 638px | 1012px | 是 ✅ |
| 1280×900 | 950px | 1012px | 是（11 列 × 92px 下限偏大，见下） |

---

## 四、没做到 / 有保留的部分（如实说明）

1. **未登录状态下无法验证"有数据"场景**：生产管理员口令是环境变量注入或随机生成，源码不硬编码，**没有尝试猜口令**。因此本轮的实测集中在结构、行为与门禁态；带真实数据的表格渲染、导出按钮点按未复测（第一批已用隔离实例验证过导出端点逻辑）。
2. **端到端 55 秒计时器长测失败**：`agent-browser` 对单次求值有读取超时（长时间 `Promise` 必然返回 `os error 10060`）。已改为"**服务器接线核对 + 同步行为实测**"闭环替代，证据链完整（见 3.3）。
3. **门禁态首帧截图曾为空白**：`agent-browser` 守护进程偶发状态异常，重新 `open` 后正常。不影响结论。
4. **测试环境需先 `body.classList.remove('locked')`**：锁定态下主区隐藏，隐藏元素**无法获得焦点**，第一次测试因此出现假阴性（`isBusyEditing` 误判 false）。修正后全部通过。这条已记入项目记忆，下次直接绕开。

---

## 五、实测顺带发现的线上真实问题

### 5.1 限流仍然打到业务端点，且**刚刚发生**

重启后日志（16:54，真实员工流量）：

```
[16:54:53] [INFO]    VISIT_CREATE pin=18335162275 name=朱林峰 id=2598
[16:54:54] [WARNING] REST_RATE_LIMITED ip=36.28.161.172 path=/api/v1/visit
[16:54:54] [WARNING] REST_RATE_LIMITED ip=36.28.161.172 path=/api/v1/auth/pending
[16:54:55] [WARNING] REST_RATE_LIMITED ip=36.28.161.172 path=/api/v1/auth/pending
[16:54:56] [WARNING] REST_RATE_LIMITED ip=36.28.161.172 path=/api/v1/auth/pending
```

**员工的上门登记请求（`/api/v1/visit`）被 429 拦下了**，同时 PC 端的授权轮询（`auth/pending`）被连续拦 3 次。这不是压测——是真实业务。根因仍是 `MAX_REST_PER_MINUTE=60` 按 **IP** 计数：一个办公室（同一出口 IP）的电话量足以打满 60/分钟。

**建议**：把限流口径改为按 **PIN**（或认证后按账号），并对 `dial` / `visit` / `calls/batch` 这类业务端点放宽或豁免。这条优先级建议提到 P0。

### 5.2 未登录时也会弹"登录已过期"

门禁态下页面初始化仍会请求数据 → 401 → 顶部红横幅出现「登录已过期，请重新登录后再查看数据」。**用户还没登录就先被告知"登录已过期"**，语义错位、且是噪音。

**建议**：`setApiBanner` 在 `document.body.classList.contains('locked')` 时直接跳过；或把文案在未登录时改为中性的"请先登录"。

### 5.3 表格最小宽度偏大（轻微）

`wrapDataTables()` 按 `列数 × 92px` 设下限，11 列 = 1012px。在 **1280×900**（很常见的笔记本分辨率）下内容区只有 950px，于是**常规屏幕上也要横向滚动**。

**建议**：改为按列内容估算或降到 80px/列，或对"操作"列之外给 `white-space: nowrap` + 自适应。属 P2。

---

## 六、回滚

```bash
sudo cp /opt/autodial/cloud_relay_v2.py.bak-20260911-batch2 /opt/autodial/cloud_relay_v2.py
sudo cp /opt/autodial/dashboard.html.bak-20260911-batch2  /opt/autodial/dashboard.html
sudo supervisorctl restart autodial
```

本地备份：`.workbuddy/backup/2026-09-11-1650/`（含改动前版本）。
验证截图：`.workbuddy/verify/2026-09-11-p0/`。

---

## 七、遗留问题清单（按优先级）

| 优先级 | 问题 | 位置 | 后果 |
|---|---|---|---|
| **P0** | REST 限流按 IP 计，业务端点被打 429（**线上正在发生**） | `cloud_relay_v2.py` `MAX_REST_PER_MINUTE` / `_rate_limit` | 员工拨不出号、登记失败 |
| **P1** | 未登录即弹"登录已过期"横幅 | `setApiBanner` 调用点 | 语义错位 + 噪音 |
| P1 | 人员管理"操作"列是空的 | `#page-pins` 表头 | 视觉噪声、像功能坏了 |
| P1 | 手机页"详情"按钮只弹 toast | `showDeviceDetail` 类函数 | 点了没内容 |
| P1 | 改密码要手输数字 ID | `#chpwd-id` | 易填错 |
| P1 | 危险操作用原生 `confirm()` | 删除账号/记录 | 样式割裂、不可访问性差 |
| P1 | 无 URL 路由，F5 回首页 | `showPage()` | 高频页每次刷新重走导航 |
| P2 | 表头/次要文字 11px、对比度 ≈4.1:1 | CSS token | 长时间值守易看错 |
| P2 | 手机页 101 台设备无分页全渲染 | `loadPhones()` | 设备变多后卡顿 |
| P2 | 表格最小宽 1012px，1280 屏也横向滚 | `wrapDataTables()` | 常规屏幕体验打折 |
| P2 | 设置页"上次刷新"提示占用同一位置，信息混用 | `#last-refresh` | 跳过刷新时的提示可能被下一次刷新覆盖 |

> 另：通话记录相关的 `call_records_raw` / `phone_events` 在生产库仍为 **0 行**——手机端从未上报过通话记录。这不是面板问题，是**上报链路**问题，建议单独立项查。

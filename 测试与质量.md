# AutoDial 测试与质量文档

> 合并自原《场景测试列表》《场景检测报告》《待验证问题》。修订：2026-08-22 | v4.14；**2026-09-01 对照实际代码逐场景复核修订**（核对文件：cloud_relay_v2.py、background.js、content-script.js、ConnectionManager.kt、DialEngine.kt、DialService.kt、ConnectFragment.kt、CallLogDb.kt、server.go、security.go、app.go、main.js）
> 关联：《CHANGELOG.md》（修复记录）、《未闭环问题清单-2026-09-12.md》（问题追踪真源；历史审计报告已于 2026-09-12 整合删除）

---

## 一、场景测试列表（50 个真实场景）

> 基于实际代码（cloud_relay_v2.py、background.js、ConnectFragment.kt、DialEngine.kt、server.go）整理。

### 1.1 网络异常（S01-S10）

**S01 — WiFi 断开，手机自动切 4G/5G**：手机已通过 WiFi 连接云中继，关闭 WiFi，等待约 10 秒后打开浏览器触发拨号。期望：云中继 90s 心跳超时内 WS 先断后重连；4G 下手机重新 ws.connect() 发送 phone_hello 带原 PIN；新 dial 正常送达拨出。

**S02 — 网络短暂波动（丢包 3~5 秒后恢复）**：路由器限速/短断 5 秒恢复，期间发起拨号。期望：WS 可能触发 write timeout 被云中继断开；手机 onFailure → 自动重连 → phone_hello；断连期间发出的拨号走 fallback 逻辑。

**S03 — WiFi-A 切 WiFi-B（同网段不同 AP）**：IP 可能变化。期望：同局域网内 WS 短暂断开重连；phone_hello 重发，云中继更新 ws 映射，旧 ws 被 close。

**S04 — PC 端网络断开，扩展检测超时走云端**：拔 PC 网线后 CRM 页面拨号。期望：扩展后台 `chrome.alarms` 每 15s 自动探测 `127.0.0.1:35432/`（500ms 超时）刷新 `pcAvailable` 缓存；探测失败 → isPcAlive() 返回 false → 自动切云中继 `/api/v1/dial` 带 X-AutoDial-PIN；下次拨号读到的仍是 15s 内的最新缓存（35s 缓存仅兜底）。

**S05 — PC 网络恢复后切回本地**：插回网线。期望：15s 内 alarms 探测成功 → pcAvailable=true → 切回本地直连；即使缓存未刷新，云端返回 `PC_CONNECTED` 时 background 也会**立即置 pcAvailable=true** 并提示"PC已上线，请重试"，下次拨号走本地。

**S06 — 云中继服务器网络闪断（3 秒）**：所有客户端 WS 同时断开；手机/PC 各自自动退避重连；重连后 phone_hello + pc_hello 重新握手；pin_groups 重建。

**S07 — 手机短暂无信号（约 30 秒）**：无信号期间 WS 断开，云中继协议层心跳（ping_interval=30s / ping_timeout=90s）判死后 finally 清理分组并向同 PIN PC 广播 phone_offline；扩展拨号返回 PHONE_OFFLINE；恢复后重连正常。（注：应用层 check_heartbeats 循环已作为死代码删除，超时判死依赖 websockets 协议层心跳）

**S08 — 弱网络服务器（1Mbps 上行，3 组 PIN）**：WS 消息正常转发但可能 100~500ms 延迟；心跳包 ~50 bytes/30s 不触发超时；dial 不因拥塞丢失。

**S09 — 手机连 VPN**：VPN 不影响 WS 连接（除非策略拦截 35430）；出口 IP 变化不影响 PIN 路由（PIN 路由不依赖 IP）。

**S10 — 4G↔5G 基站切换**：底层 TCP 不中断则 WS 保持；IP 变化导致 TCP 重置则 WS 断开自动重连；拨号不受影响。

### 1.2 设备状态切换（S11-S18）

**S11 — Home 退后台再切回**：DialService 是前台服务（startForeground + 通知 + PARTIAL_WAKE_LOCK），WS 后台活跃；onResume 仅读取 DialService.isConnected 刷新 UI（断开时的重连由 ConnectionManager 自动调度，非 onResume 触发）。

**S12 — 锁屏后唤醒（60 秒）**：锁屏期间 WS 30s 心跳保持 + WakeLock（12h PARTIAL_WAKE_LOCK）持锁；唤醒后 onResume 刷新状态；锁屏期间 dial 成功投递，解锁后见记录。

**S13 — 系统强杀后重启**：强杀时 WS 无 close frame，云中继协议层心跳判死后清理；重开 App → onResume 自动连接；BootReceiver 不应触发（非重启）。

**S14 — MIUI/HyperOS 电池优化后台断连**：电池优化限制后台网络 → WS 心跳停止 → 协议层心跳判死断开；解锁后自动重连；App 内「电池优化」显示"受限"引导白名单。

**S15 — 系统重启自动启动**：BootReceiver 收 BOOT_COMPLETED → 启动后台 Service → 读保存的 PIN + 服务器 → 自动 ws.connect() → phone_hello，无需手动开 App。

**S16 — 勿扰模式**：不影响 WS 连接、消息接收与拨号；通话记录正常写入。

**S17 — 低电量省电模式**：省电可能冻结后台网络，WS 心跳可能超时；前台时连接保持；App 内提示用户调整。

**S18 — 横竖屏旋转**：onDestroyView → onResume 生命周期正常；WS 在 ConnectionManager/Service 层不受 Fragment 重建影响。

### 1.3 PC 端场景（S19-S25）

**S19 — PC 未开机，扩展全程云端**：isPcAlive() 超时 → 云中继检查 pin_groups[pin].pcs 空 → 查 phones → 转发 → ACCEPTED，手机正常拨出。

**S20 — PC 休眠后唤醒**：休眠期间协议层心跳停 → 云中继断开 PC WS 并向手机广播 pc_offline；唤醒后 Go PC 自动重连 pc_hello，手机收 pc_online；扩展 15s 探测恢复后切回本地直连（或云端 PC_CONNECTED 立即置位触发切回）。

**S21 — 端口 35432 被占用**：Go 端 `http.Server.ListenAndServe()` 返回错误 → `pushToRenderer("server-log")` 提示「端口 35432 被占用！请先关闭旧版 AutoDial 再启动本程序」，**进程不退出**（HTTP 服务缺失，云连接仍工作）；Electron 端 EADDRINUSE → `dialog.showErrorBox` 弹窗。关闭冲突进程后重启正常。

**S22 — Go + Electron 双端同时运行**：后启动端 bind 失败提示；扩展 isPcAlive() 连到先启动端；只有一个 PC 端工作。

**S23 — 防火墙未放行 35432**：netsh 失败仅影响局域网外部连接；127.0.0.1 本地不受防火墙拦截（扩展可达）。应用层还有 security.go 来源校验：扩展请求带 `chrome-extension://` Origin 可信通过，外部网页 Origin 403 拒绝。

**S24 — CPU 满载消息延迟**：TCP 保证消息不丢失，延迟可能增加；延迟 > ping_timeout 90s 才会被判死断开，恢复后重连。

**S25 — 长时间空闲（>1 小时）**：30s ping/pong 心跳 < 90s ping_timeout 判死阈值，连接保持；突然拨号立即响应。

### 1.4 号码类型（S26-S32）

**S26 — 11 位手机号（13800138000）**：content-script 正则 `1[3-9]\d{9}` 识别 → 云端 `/api/v1/dial` 清理后 11 位（3~20 位区间）→ ACCEPTED → DialEngine 原样拨出。

**S27 — 客服短号（10000/10086）**：5 位纯数字，云端校验通过（扩展自动检测覆盖不到 5 位号码，需 PC 端/拨号盘手动发起）。

**S28 — 带区号座机（010-12345678）**：云端路径清理 `-`、空格、`+`、`*`、`#`（**不含括号**）→ "01012345678"（11 位）→ 通过；PC 本地直连路径 `isValidDialNumber()`（Go devices.go / Electron network.js）清理 `空格 - ( ) * #` 后同样通过；**转发/呼叫的都是原始号码**（含 `-`）。注：原文档引用为 Android 端函数，实际 `isValidDialNumber` 位于 Go/Electron PC 端，号码校验在云端与 PC 端两层。

**S29 — 400 号码（400-800-8800）**：云端清理后 "4008008800"（10 位）→ 通过；原始号码转发拨出。

**S30 — 带 \* # 服务号（\*100#）**：云端清理 `*`/`#` 后 "100"（3 位）→ 通过；Go PC 端清理后同通过；**Electron 端正则 `/^(\+?[\d\s\-\(\)]{4,20})$/` 不含 `*`/`#` → 本地直连路径被拒**（端间差异 ⚠️，需走云端）；最终转发原始号码 `*100#`，手机 `tel:` 呼叫触发 USSD。

**S31 — 国际号码（+8613800138000）**：云端清理 `+` 后 13 位 → 通过；原始号码（含 +）转发拨出。

**S32 — 含空格号码（"138 0000 1111"）**：content-script 正则仅匹配连续 11 位数字，含空格不会被自动检测；手动输入时云端清理空格后校验通过（仍拨原始号码）。⚠️ 潜在问题：自动检测覆盖不到含空格格式。

### 1.5 Chrome 扩展场景（S33-S39）

**S33 — 多 Tab 各点一个号码**：各 content-script 独立发消息 → background 依次处理；云端 5s 去重仅同号码，不同号码并发通过。

**S34 — PIN 自动检测误匹配**：优先 CSS 选择器 `.user-phone` 精确提取，失效后回退 TreeWalker 扫描取第一个 `1[3-9]\d{9}`；订单号/身份证先出现可能误匹配；popup 可手动修正。⚠️ 设计取舍（未改）。

**S35 — Popup 打开期间拨号**：popup 不影响 content-script 监听；background 经 alarms 15s 探测 + onMessage 唤醒 service worker，dial 正常。

**S36 — 扩展自动更新后需刷新页面**：旧 content-script 已卸载，新版需刷新 CRM 页面注入；刷新前点击不触发拨号（Chrome 机制，非代码问题）。

**S37 — 多 Chrome Profile 装扩展**：独立 storage 与 service worker；同 PIN 下作为两个独立 PC 客户端连接云中继，均可能收到 dial_result。

**S38 — storage 中 PIN 为空**：getPin()（`pin || self_phone` 兜底）返回空 → dial 提示「未检测到坐席手机号，请打开 CRM 页面」、登记提示「PIN未设置，请先打开CRM页面检测坐席手机号」、挂断返回「PIN 未设置」，不发送无效请求。

**S39 — 服务器地址不可达**：fetch 超时/异常 catch → 提示「网络错误，请检查云端服务器」（服务器列表拉取 8s 超时），不阻塞后续请求。

### 1.6 Android 端场景（S40-S47）

**S40 — 双 SIM 卡拨号弹出选卡**：DialMode 共 7 种（POPUP 弹窗选卡 / ROUND_SELECT 智能轮选 / OPPOSITE 相反 / SIM1 / SIM2 / ALTERNATE 循环 / SYSTEM 系统默认）。POPUP（resolveSimSlot=-1）弹自定义 SimSelectOverlay 悬浮窗（无悬浮窗权限则广播 ACTION_SHOW_SIM_SELECT 由 Activity 兜底）→ 选卡后发 DIAL_WITH_SIM 广播 → DialService 调 performDial 拨出；SYSTEM（-2）ACTION_CALL 系统默认 SIM；指定 SIM 槽 + 小米设备 `DialAccessibilityService.expectSimPicker()` 预布无障碍自动点击系统选择器；performDial 先 placeCall，失败回退 ACTION_CALL。

**S41 — SIM1 无信号（轮选模式）**：ALTERNATE/ROUND_SELECT 轮选到 SIM1 失败 → 系统提示无网络；下次轮选 SIM2 成功；失败记录写入 CallLogDb（insertDial error）。注：ROUND_SELECT 为「智能轮选」——同一号码 10 天内拨过则返回 -1 弹窗选卡，否则全局交替；resolveSimSlot 仍不查 SIM 可用性。⚠️ `resolveSimSlot()` 不检查 SIM 可用性（低风险未改）。

**S42 — 无障碍未开启（SIM1 模式）**：小米设备弹系统 SIM 选择框无法自动点击，拨号停留；需手动开启无障碍服务（无 App 内自动提示）。

**S43 — 通话中收到新 dial**：`dialNumber()` 不检查当前通话状态，直接呼叫新号码可能失败；endCall() 仅由 hangup 触发。⚠️ 低风险未改。

**S44 — 拨号后立即挂断**：扩展发 /api/v1/hangup → 云端转发 → DialEngine → TelecomManager.endCall() 取消呼出/挂断。

**S45 — 存储不足记录日志**：CallLogDb.insertDial() 已 try-catch，磁盘满仅 Log.e 不崩溃。

**S46 — 手机时间与服务器不同步（>5 分钟）**：WS 不校验时间戳；通话日志以手机本地时间为准；功能不受影响。

**S47 — 连续快速拨 5 个不同号码（<1 秒间隔）**：background 依次发送；云端 5s 去重按**原始号码字符串**记录（不同号码互不影响）按序转发；手机端无队列，消息到达即调 dialNumber → placeCall 并发发起（交由系统 TelecomManager 排队）；DUPLICATE_DIAL 不误触发（号码不同）。

### 1.7 并发与竞争（S48-S50）

**S48 — 同 PIN 双手机连接**：B 连接时云中继在 phone_hello 中无条件 close 旧手机 ws（4001 duplicate_reconnect），无论 device_name 是否相同 → 同 PIN 仅一台手机在线。前提：设备须已在云端注册（device_name 有默认 PIN），未注册设备直接 auth_fail 拒绝；默认 PIN 不匹配则走扩展端授权流程（auth_pending → auth/respond），授权通过后才入组。

**S49 — PIN 修改期间收到拨号**：PC 端 `/api/set-pin` 修改 PIN 时主动断开旧 PIN 全部设备连接（LAN/Cloud WS）并清空 devices；断连窗口内拨号按 server.go /dial fallback 走云端唤醒或排队。

**S50 — 云中继重启瞬间**：旧进程 try-except 向每连接 ws.close(1001)；新进程 pin_groups 为空；客户端重连触发重新握手；重启期间 REST dial 返回 PHONE_OFFLINE。

---

## 二、场景检测报告（对照代码审计结论）

> 基于 50 个场景逐一对照代码审计：**45 ✅ 通过 / 5 ⚠️ 潜在问题 / 0 ❌ 缺失**。

### 2.1 网络异常 — 全部通过 ✅

| 场景 | 代码依据 |
|------|---------|
| S01 WiFi→4G | `NetworkMonitor.onLost/onAvailable` → `scheduleNetworkDebounce` → `scheduleReconnect("network_change")` → 阶梯退避 0s/1s/3s/5s/10s/30s/60s/300s（LAN 上限 30 次、Cloud 上限 8 次） |
| S02 短暂波动 | WS onFailure/onClosed 触发重连；云中继协议层心跳（ping_interval=30s/ping_timeout=90s）判死清理，重连后 PIN 重新映射 |
| S03 AP 漫游 | TCP 断开被 WS 层捕获 → handleLan/CloudDisconnect → 自动重连 |
| S04 PC 断网 | alarms 15s 后台探测（PC_PING_TIMEOUT=500）刷新缓存 → isPcAlive() false → fallback 云端；35s 缓存仅兜底 |
| S05 PC 恢复 | 15s 探测恢复 → pcAvailable=true；云端 PC_CONNECTED 也立即置位触发切回 |
| S06 云中继闪断 | 优雅关闭 `stop_server()` 遍历 `ws.close(1001, 'server shutting down')`；客户端 onClosed 自动重连 |
| S07 无信号 | 协议层心跳 30s/90s 判死 → finally 清理分组 + 广播 phone_offline；恢复后 NetworkMonitor.onAvailable 触发重连 |
| S08 弱网络 | WS 心跳 ~50 bytes/30s，90s 容忍度足够；TCP 保证 dial 送达 |
| S09 VPN | WS 路由基于 PIN 不依赖 IP；除非 VPN 拦截 35430 |
| S10 基站切换 | TCP 层透明，除非 IP 变化导致重置 → onFailure 重连 |

### 2.2 设备状态 — 1 个潜在问题 ⚠️

| 场景 | 状态 | 代码依据 |
|------|:---:|---------|
| S11 Home 退后台 | ✅ | Foreground Service（startForeground + 通知 + PARTIAL_WAKE_LOCK 12h），WS 后台活跃；onResume 仅刷新 UI |
| S12 锁屏唤醒 | ✅ | WakeLock 持锁 + WS 30s 心跳保持，onResume 刷新 |
| S13 系统强杀 | ✅ | onDestroy 不调；下次启动 loadSavedConfig() 自动重连 |
| S14 电池优化 | ✅ | ConnectFragment 检测并引导白名单（未加白弹窗引导）；受限时仍可能断连 |
| S15 系统重启 | ✅ | BootReceiver(BOOT_COMPLETED) → startForegroundService → onCreate loadSavedConfig() 自动 connect |
| S16 勿扰模式 | ✅ | WS + 拨号不受勿扰影响 |
| **S17 低电量省电** | ⚠️ | **省电模式限制后台网络可能冻结 WS 心跳 → 协议层 90s 判死断开**；App 内仅提示"受限"无代码级保活 |
| S18 横竖屏旋转 | ✅ | WS 在 ConnectionManager 中，不受 Fragment 重建影响 |

### 2.3 PC 端 — 全部通过 ✅

| 场景 | 代码依据 |
|------|---------|
| S19 PC 未开机 | alarms 探测失败 → 云端 pcs 空 → 查 phones → 转发 → ACCEPTED |
| S20 PC 休眠唤醒 | 休眠协议层心跳停 → 云中继断开 + 广播 pc_offline；唤醒后 reconnect → pc_online |
| S21 端口占用 | Go ListenAndServe() error → pushToRenderer("端口 35432 被占用！...")，进程不退出；Electron dialog.showErrorBox |
| S22 双 PC 端 | 后启动 bind 失败提示；先启动端正常工作 |
| S23 防火墙未放行 | netsh 失败仅影响局域网外部连接；扩展请求带 chrome-extension:// Origin 通过 security.go 来源校验 |
| S24 CPU 满载 | TCP 不丢消息；延迟 > ping_timeout 90s 才判死 |
| S25 长时间空闲 | 30s ping/pong < 90s 判死阈值，连接保持 |

### 2.4 号码类型 — 1 个潜在问题 ⚠️

| 场景 | 状态 | 代码依据 |
|------|:---:|---------|
| S26 11位手机号 | ✅ | content-script `1[3-9]\d{9}` → 云端清理后 3~20 位通过 → 原样转发拨出 |
| S27 10000 短号 | ✅ | 云端清理后 5 位（≥3 位）通过；自动检测覆盖不到，需手动发起 |
| S28 区号座机 | ✅ | 云端清理 `-`/空格/`+*#`（无括号）→ 11 位通过；原始号码转发 |
| S29 400号码 | ✅ | 云端清理后 10 位通过 |
| S30 USSD(*100#) | ✅ | 云端清理 `*#` 后 "100" ≥3 位通过；转发原始 `*100#`，手机 tel: 触发 USSD |
| S31 国际号码 | ✅ | 云端清理 `+` 后 13 位通过 |
| **S32 含空格** | ⚠️ | content-script 正则仅匹配连续数字，含空格号码无法自动检测；手动输入时云端清理空格后校验通过；setPin 已 trim + 校验 4/11 位 |

### 2.5 Chrome 扩展 — 1 个潜在问题 ⚠️

| 场景 | 状态 | 代码依据 |
|------|:---:|---------|
| S33 多 Tab | ✅ | 独立 content-script → sendMessage → background 统一处理；5s 去重仅同号码 |
| **S34 PIN 误匹配** | ⚠️ | 优先 `.user-phone` 选择器，失效后回退 TreeWalker 取第一个匹配；订单号/身份证先出现仍可能误匹配；popup 可手动修正（设计取舍） |
| S35 Popup 打开拨号 | ✅ | alarms 15s 探测 + onMessage 唤醒 service worker |
| S36 扩展更新 | ✅ | Chrome 机制：需刷新页面注入新版 |
| S37 多 Profile | ✅ | 独立 storage/worker；同 PIN 各自建立 pc_hello |
| S38 PIN 未设置 | ✅ | getPin() 返回空 → 提示，不发无效请求 |
| S39 服务器不可达 | ✅ | fetch 超时 catch → 提示，不阻塞 |

### 2.6 Android 端 — 2 个潜在问题 ⚠️

| 场景 | 状态 | 代码依据 |
|------|:---:|---------|
| S40 双卡选卡 | ✅ | SYSTEM→ACTION_CALL；POPUP→SimSelectOverlay→DIAL_WITH_SIM 广播→performDial；指定槽→placeCall + 小米 expectSimPicker 无障碍自动点击 |
| **S41 SIM1 无信号** | ⚠️ | **resolveSimSlot() 不检查 SIM 可用性**，直接返回槽位，placeCall 可能失败/超时 |
| S42 无障碍未开启 | ✅ | SIM1/SIM2 模式小米弹系统选择框无法自动点击，停留等待手动操作 |
| **S43 通话中收到 dial** | ⚠️ | **dialNumber() 不检查当前通话状态**，直接呼叫新号码可能失败 |
| S44 拨号后立即挂断 | ✅ | hangup → endCall() 取消呼出/挂断 |
| S45 存储不足 | ✅ | insertDial() try-catch 保护，磁盘满仅 Log.e |
| S46 时间不同步 | ✅ | WS 不校验时间戳；日志以本机时间为准 |
| S47 连续快速拨号 | ✅ | 不同号码 5s 去重不误触发；手机逐号处理 |

### 2.7 并发与竞争 — 全部通过 ✅

| 场景 | 代码依据 |
|------|---------|
| S48 同 PIN 双手机 | phone_hello 无条件踢旧手机（close 4001 duplicate_reconnect），与 device_name 无关 |
| S49 PIN 修改中拨号 | set-pin 断开旧 PIN 全部设备并清空 devices；窗口内拨号走云端唤醒/排队 |
| S50 云中继重启瞬间 | shutdown() → close(1001)；重启期间 REST dial 返回 error；恢复后重连 |

### 2.8 问题汇总

**⚠️ 建议修复（2 个，均为低严重度，属设计取舍未改）**：

| 编号 | 文件 | 问题 | 建议 |
|------|------|------|------|
| B3 | DialEngine.kt | dialNumber() 不检查当前通话状态（2026-09-01 复核确认仍存在） | 调用前检查 TelecomManager.isInCall |
| B5 | DialEngine.kt | resolveSimSlot 不检查 SIM 可用性（2026-09-01 复核确认仍存在） | 可选加 SubscriptionManager 状态检查 |

> 原 B1（insertDial 无 try-catch）、B2（setPin 未清理空格）、B4（同 PIN 不同 device_name 不踢旧连接）经核实已修复，从建议列表移除。
> 2026-09-01 复核备注：云中继应用层心跳检查 `check_heartbeats` 已作为死代码删除（CHANGELOG Q4），90s 判死现完全依赖 websockets 协议层心跳（`ping_interval=30, ping_timeout=90`）；Android 端无号码清理函数，号码校验在云端 `/api/v1/dial`（清理 `+ * # - 空格` 后 3~20 位，无括号清理，转发原始号码）与 PC 端 `isValidDialNumber`（Go/Electron，清理 `空格 - ( ) * #`）两层；**Electron 端 `isValidDialNumber` 正则不含 `*`/`#`，USSD 号码（如 `*100#`）走 Electron 本地直连会被拒，仅云端/Go 路径支持**（新增端间差异，低危）。

**ℹ️ 注意事项**：N1 S17 低电量省电可能冻结 WS → 用户需加电池白名单；N2 S34 PIN 误匹配 → 用户手动修正。

**v4.14 后新增覆盖项**：PC 云端唤醒离线手机（reconnect_request 转发白名单）、设备默认 PIN 不被抹空（ON CONFLICT DO UPDATE）、扩展端授权流程（auth/respond NameError + 归属校验）、敏感读端点鉴权、PC 本地端口来源校验——均已修复，见《AutoDial技术文档.md》与 CHANGELOG。

### 2.9 压测实测（50 并发，2026-08-31 线上）

> 方式：服务器本机（`101.34.65.254`，4 核 / 3.3G 内存 / Python 3.10）跑 `cloud-relay/python/test_stress_50_users.py`，改端口连**隔离实例 35499 + 独立测试库**，未污染生产 `visits.db`、未触发真实拨号。已部署为新版 `cloud_relay_v2.py` + `dashboard.html`（v6.0）。

| 阶段 | 结果 |
|------|------|
| 手机 WS 连接 | **50/50 (100%)**，1.2s，P50=8.4ms P99=12.2ms |
| PC WS 连接 | **50/50 (100%)**，1.1s |
| REST 拨号（50 并发） | **50/50 (100%)**，P50=27ms P99=35ms |
| 并发登记（20） | **20/20 (100%)**，P50=51ms P99=84ms |
| 并发查询（20） | **20/20 (100%)**，P50=16ms P99=23ms |
| WS 消息压测（100 连接） | P50=5.2ms **P99=8.3ms**（3 轮全过） |
| PC 在线 REST 拨号 | 20/20 正确返回 `PC_CONNECTED` |

**结论：压测断言 5/5 全部通过 ✅**（手机/PC 连接 100%、消息 P99<500ms、REST 拨号 P99<500ms、PC 在线拨号 P99<1000ms）。压测实例 CPU 0.7%、内存 1.1%，服务器负载极低。测完已清理隔离实例与测试库。

---

## 三、待验证问题清单（QA）

> 每次构建/部署后按此清单逐项验证，通过打 ✅，失败打 ❌。B1-B5 与 A1-A3 已于 2026-08-22 分批修复并经 QA 独立回归（17 PASS/1 风险、12/12 PASS、R1-R6 PASS、13/13 PASS），详见 CHANGELOG.md。

### 3.1 代码审计复审项（2026-08-21，修复后状态）

**B1. Android 14 上 DialService 是否启动即崩** — ✅ 已修复（AN-P0-2：亮屏 receiver 补 RECEIVER_NOT_EXPORTED）。验证：Android 14+ 真机安装启动 → `adb logcat -s AutoDial:D` 观察无 SecurityException；前台通知正常、亮屏健康检查工作。

**B2. 设备授权弹窗（auth.html）在扩展中是否可用** — ✅ 已修复（EX-P0-1：内联脚本抽到外部 auth.js 规避 MV3 CSP；v4.14 auth/respond 携带 PIN + 归属校验）。验证：手机非默认 PIN 登录 → CRM 页面 → 5 秒内授权弹窗出现、显示设备名/PIN、按钮可用。

**B3. 恶意网页能否静默拨号** — ✅ 已修复（GO-P0-1：空/null Origin 拒绝 + 精确 host 校验；Electron EL-P1-4 同步收紧；HTTP/WS 全覆盖）。验证：含 `<img src="http://127.0.0.1:35432/dial?number=10086">` 的 `localhost.evil.com` 页面 → 不触发拨号。

**B4. 多云服务器配置下故障转移** — ✅ 已修复（EL-P0-1：failover generation 单次递增）。验证：两台云服务器第一台不可达 → 重启 PC 端 → 自动切第二台。

**B5. 云通道拨号是否 6 秒超时且重复拨号** — ✅ 已修复（GO-P0-3：cloud.go 读循环补 case "ack" 与本地共用 handleAck）。验证：手机走云中继 → 扩展拨号 → /dial 正常返回不误超时、手机不重复拨号。

### 3.2 v4.14 新增项（2026-08-19）

**A1. 扩展端设备授权流程** — ✅ 已修复（auth/respond 不再 NameError、返回 auth_ok；QA 回归通过）。验证步骤：面板预设默认 PIN → 设备用不同 PIN 登录收 auth_pending → 扩展弹窗点允许 → 手机收 auth_ok。

**A2. 管理员敏感端点鉴权** — ✅ 已实现（敏感读端点全部鉴权；dashboard 登录限频；QA 回归通过）。验证：未登录访问 `/api/v1/pins` → 401 UNAUTHORIZED；登录带 ?token= → 200；手机端 ?pin= 同步不受影响。

**A3. PC 端本地端口来源校验** — ✅ 已实现（Go/Electron 双侧对齐；HTTP 与 WS 覆盖；QA 回归通过）。验证：扩展拨号正常；直接打开 `http://127.0.0.1:35432/` 正常；外部网页 fetch dial → 403。

### 3.3 功能待验证项（手机端/插件端/云端）

| # | 项 | 状态 |
|---|----|------|
| 1 | 录上门后统计详情弹窗：录上门完成登记 → 统计页点「今日/本周」数字 → BottomSheet 显示客户姓名/手机号/登记时间 | ⏳ 待真机验证 |
| 2 | 云端同步上门数据：统计页「🔄 同步」→ 可达 Toast「同步完成共 X 条」/ 不可达「同步失败」 | ⏳ 待真机验证 |
| 3 | 顾问姓名选择器：录上门「接待顾问姓名」点击弹出 CRM 顾问列表，选中自动填入 | ⏳ 待真机验证 |
| 4 | CORS 头与鉴权：访问 35430 面板 API 不被浏览器拦截；未登录敏感接口 401 | ⏳ 待验证 |
| 5 | CRM 不再双重同步：插件/手机端登记后 CRM 只收一条 | ⏳ 待验证 |

> 相关修复参考：录上门详情弹窗/登记成功写入 visit_records（RegisterFragment.saveVisitRecord → StatsFragment.loadVisitStats/showVisitDetail）；云端同步（StatsFragment 同步按钮）；顾问选择（/api/v1/advisor/name）。
